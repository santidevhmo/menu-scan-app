⛔ RETIRED — DO NOT USE THIS AS AN ENTRY POINT.

This file was the kickoff prompt for the HORIZONTAL MENUS phase, which CLOSED on 2026-08-04
(ledger evals 132-137: H2 rotation deployed as edge fn v22 and device-verified). Its contents
described that phase's read-order, status and next actions, all of which are now history.

    NEW SESSIONS START HERE:  docs/superpowers/START-HERE.md

That file routes you to the current phase, which is recorded in exactly one place — the
"🎯 CURRENT PHASE" block at the top of:

    docs/superpowers/plans/2026-07-04-ocr-extraction-master-roadmap.md

Why this file is a stub rather than deleted: the 141-entry iteration ledger references this path,
and the sibling files in this folder, throughout its history. The paths stay valid; the guidance
does not.

The durable, phase-independent parts of the old prompt — Santiago's standing rules on cost
approval, oracle changes, skill usage and report style — were moved into START-HERE.md so they
survive this phase's retirement. Nothing was lost; see git history for the original text
(`git log --follow -- docs/superpowers/horizontal-menus/DELEGATION-PROMPT.txt`).
