# Horizontal Menus Roadmap (critical-path #4) — dedicated plan

> **What this is:** the working plan for critical-path #4 (horizontal/landscape menu handling), structured like the master roadmap: phases with cheap-probe-first iteration, frozen-gate discipline, and user rulings at checkpoints. Master roadmap: main repo `docs/superpowers/plans/2026-07-04-ocr-extraction-master-roadmap.md` ("Release scope decision" item 4). Read its **Strategy Rules** and **Lessons learned** before any phase.

**Status:** ACTIVE 2026-07-22 · **🅿 NEW PRIORITY ZERO (ruling 29, 2026-07-22): GPT-4o→Mistral-OCR EXTRACTION MIGRATION.** Santiago redirected: make Mistral OCR (native `document_annotation_format`) the Stage-1 EXTRACTOR, matching the eval093/H1.3 GPT-4o quality on all fixtures, with GPT-4o kept for enrichment only. Evidence (eval 095, B1 probe, $0.045): one Mistral annotation call, NO tiling → full item recall ×3 all 3 wide menus, ~0 inventions, perfectly deterministic ×3, names/prices ≥ GPT; gap = Mistral's own artifact shape needs a Mistral-specific cleanup layer (the GPT-tuned postprocess doesn't clean it). **This reframes the horizontal roadmap:** if Mistral reads the whole image, H1 tiling + the colocation stage likely become MOOT; rotation (H2) is still needed. **PROGRESS: M0 (B1 feasibility) ✅ + M1 (cleanup layer) OFFLINE GATE MET 2026-07-22 (`7f8446c`): polloteria ALL-5 ×3 (ruling-6 clean), GH recall 48/48 + zero-invention ×3, bistro 4/5 (only the E misattached-add-on item, a deliberate M2 fix). NEXT ACTION = M2 — the scored ×3 LIVE gate (fresh cost approval ~$0.05–0.15): re-run Mistral B1 fresh with the general E extraction-instruction fix baked into the `document_annotation` option-field description ("options must be printed choices on THAT item's own card; never turn a general 'add to your X' note into options on an unrelated item"), score all 5 dims + ruling-6 audit ×3, and REGRESSION-CHECK it doesn't harm polloteria/GH (Santiago's rule: menu-generic + archive-tested prompt changes). Then M3 edge wiring, M4 all-9. See the "🅿 PRIORITY ZERO" section + the brief's newest PROGRESS LOG entry.**
> **Prior status (H1–H4 horizontal launch plan — PARKED behind Priority Zero, but H1 stays CLOSED):** Foundation ✅ (fixtures; tile-extraction clean on dense eval 085; ruling 24 tile-all; 1f PARKED). **H1 (recall-safe tiling) CLOSED 2026-07-21 (eval 093):** all 3 wide menus recall-clean ×3 through the landscape 0.65/0.35 tile geometry; polloteria all-5-dims ×3; GH 48/48 recall incl. all 12 Enhancements + 3 Sides, zero inventions. **H2 rotation — mechanism FOUND (eval 094, this session)** but PARKED behind Priority Zero: OCR/on-device block-geometry is a clean single-shot direction-aware detector (`wide-frac` axis + `sign(corr(readingOrder,X))` polarity); resume at H2.2 build. H3 wiring → H4 combined 9-menu gate still after. Do NOT re-probe nested geometry or prompt-based dense detection (both dead — evals 072, 086–088).
**Working directory:** the worktree `/Users/santiagoaguirre/Desktop/CODING/menu-scan-app/.worktrees/extraction-eval-harness`, branch `feat/extraction-eval-harness` (pushed to origin). MOVED 2026-07-17 out of `/private/tmp`: macOS reaps /private/tmp files unused ≥3 days every night (`tmp_cleaner` 00:00 + `dirhelper` 03:35, CLEAN_FILES_OLDER_THAN_DAYS=3) and wipes it on reboot — the root cause of both worktree-loss incidents. Main checkout is device-builds only; never commit there (docs excepted).

## Division of labor (formalized from ticket #3, which closed under this model)

- **Planner/orchestrator LLM (this plan's author):** analysis, brainstorming, probe design, dump diagnosis, oracle adjudication support, gate judgment, ledger/diagram/roadmap/memory writes, task-block authoring.
- **Executor LLM:** receives self-contained task blocks pasted from here (zero-context format: exact code, exact commands, expected outputs). Owns: writing code, TDD self-checks, `deno check`/`tsc`, commits, launching detached runs, monitoring logs, reporting raw results back. **Hard rules for every task block:** never launch a multi-run gate without an explicit user cost approval relayed in the block; never commit in the main checkout; push `feat/extraction-eval-harness` to origin after EVERY commit (the 2026-07-17 rescue found 3 stranded unpushed commits); stop at checkpoint lines and report — checkpoints are the planner's.
- **User:** oracle rulings (ORACLE-CHANGEs are user-only), convention rulings, cost approvals, device scans.

## Evidence base (2026-07-13, ledgered as eval 059 context)

Three landscape-upright fixtures photographed/collected by the user, all in `/Users/santiagoaguirre/Downloads/MenusTesting/`, drafts extracted via `scripts/extract-draft.ts` and hand-corrected by the user in place (`*.draft.json`, user edits 2026-07-13 19:00–20:01):

| Menu | File | Dims | Draft result | Character |
|---|---|---|---|---|
| Bistro | `BistroMenu.png` | 2384×1844 | 26 items, structure right, ~±10MXN price misreads (user-fixed) | least dense; near-pass |
| Polloteria | `PolloteriaMenu.png` | 2274×1572 | 42 items where manual count says ~40 food + 15 drink entries — badly under-extracted, names mangled (El Tendedero→"El Tenderete", Megacharola→"Megacharlones", Papabones!→"PapaBoneless"), **dense detector did NOT fire** | dense; the star failure |
| GuestHouse | `GuestHouseMenu.png` | 2606×1580 | 49 items, cleanest; Enhancements column-pairing price misreads; per-oz/per-size pricing open | English (bake-off asset) |

User manual category counts (fixture truth): Polloteria food = Entradas 6, Pa'Compartir 3, Ensaladas 3, Crispy Chicken 5, Especialidades 5, Sandwiches & Hamburguesas 10, Sides 4, Pollo Kids 4 (= 40 food); drinks = Bebidas 14 + Malteadas 1 (6 options) — drinks are F5-deferred, parked in fixtures, NOT gated. GuestHouse = Raw Bar 6, Starters 4, Salads 4, Butcher's Best 5, Enhancements 12, Entrees 8, Sides 8 (49 incl. Seafood Plateau handling per ruling below).

Key diagnostic facts:
1. **All 3 photos are landscape-UPRIGHT** (readable text, wide). They exercise wide-layout handling. The roadmap's #4 trigger scenario — portrait photo with the menu rotated 90° — is exercised by ROTATED COPIES of these same photos (Phase 3); one oracle serves both arms.
2. **The dense detector returned dense=false on Polloteria** despite classic overload symptoms (under-count + name mangling). Landscape-dense is a detector blind spot; the failure-as-signal branch (timeout/length) also never fired.
3. **Misreads correlate with the overloaded single-call read.** Nikkori precedent: names distorted in whole-page reads came back clean from tiles. Expectation: fixing dense-landscape (Phase 1) fixes most misreads for free. Residual stable misreads = the deliberately-ungated name-spelling class → owned by the post-release name-verification pass / model bake-off (Polloteria joins the bake-off fixture set: Spanish + dense + stylized names).
4. **~90% of size-variant items lost their variant text entirely** (model dropped "G (300gr) $179" halves, filled `price` with one value). NOT postprocess-recoverable (lesson #6: never-emitted text has no downstream lever) → Phase 2 is extraction-convention work, not a parser.

## Architecture decisions (locked with user 2026-07-13)

- **ONE pipeline, no horizontal fork.** Orientation is an input property. No separate edge function/stage. Landscape behavior enters only as: (a) client-side upright rotation before upload (pipeline never sees rotated pixels); (b) aspect-ratio-parameterized dense-detection/tile-geometry inside the existing functions. A unit test pins *portrait input ⇒ byte-identical behavior to today* — vertical safety is proven by construction during iteration, not by burning gates.
- **Mode/aspect-scoped prompt changes only** (master-roadmap lesson from v6/v7): any P1 sentence added for landscape evidence must be scoped to where it was measured, never global, unless probe evidence justifies global.
- **Cost policy:** iterate on `EVAL_MENUS`/single-menu probes ($0.05–0.30) + offline re-scoring of archived dumps and the 3 corrected drafts ($0). The 6-menu vertical suite re-runs live ONLY at the Phase-4 combined exit gate (9 menus ≈ $1.35–1.50/attempt, user-approved per attempt).

## Rulings

29. ✅ **PRIORITY ZERO — GPT-4o→Mistral-OCR extraction migration (user, 2026-07-22):** the Stage-1 extractor changes from GPT-4o Vision to **Mistral OCR native structured extraction** (`document_annotation_format` = our menu schema, ONE call). Goal: match the eval093/H1.3 GPT-4o quality — all 5 frozen dims ×3 + ruling-6 audit, on the release-critical fixtures — with Mistral as the extractor. GPT-4o is retained ONLY for Stage-2 enrichment. This is now the top priority, ahead of rotation (H2) and the rest of the horizontal launch plan. Evidence = eval 095 (B1 probe): Mistral gives full item recall in one call (NO tiling), ~0 inventions, perfect determinism ×3, names/prices ≥ GPT; the only gap is a Mistral-specific deterministic cleanup layer (the GPT-tuned `postprocessItems` doesn't clean Mistral's self-echo-option / Shape-A-split / section-spacing artifacts and even regresses polloteria). **Consequences to evaluate during the migration:** (a) H1 recall-safe tiling likely becomes MOOT (Mistral reads the whole 2048 image cleanly — no tile-seam recall loss); (b) the colocation anti-invention stage (ruling 15) likely becomes MOOT (Mistral transcribes → structurally can't invent); (c) rotation (H2) is STILL required (upright input still better — prices degrade sideways, eval 094); (d) the migration must re-prove ALL 9 menus incl. the 6 CLOSED vertical gates — Mistral becomes the extractor for EVERY menu, so this is the biggest regression-scope risk (the incumbent GPT gates do NOT transfer for free). Supersedes ruling 20's "post-release bake-off" framing for the Mistral-vs-GPT extractor question specifically — that comparison is now a pre-release migration, not a parked bake-off item.
1. ✅ **Size-variant fold shape = Shape A (user, 2026-07-13):** card = smallest printed size (its price+grams); other sizes = priced options with grams in the option name (`{"name":"Ensalada Boneless","price":158,"grams":150,"options":[{"name":"G (300gr)","price":179,"grams":300}]}`). Covers Alitas 6/12/20 PZ, GuestHouse Parmesan filet 7oz/10oz 65/85, Seafood Plateau FOR TWO 150 / FOR THREE+ 280. Tomahawk 6.50 PER OZ = per-unit pricing (PER_UNIT_NOTE family), separate micro-ruling at Phase 2 if it surfaces. **Shape-A option pins enter the fixtures at PHASE 2 KICKOFF, not Phase 0** — the corrected drafts (= Phase-0 truth) still carry variant text embedded in titles, so pinning Shape A now would break fixture↔draft self-consistency; Phase 2's exit is precisely closing that gap.
2. ✅ **Polloteria aderezos prose option = `unchecked: true`** (user, 2026-07-13; Plato Surtido precedent) on the Cubeta Pollotera entry.
3. ⏳ **Detector strategy (after Phase 1 diagnosis):** prompt-rule vs deterministic assist (aspect ratio + expected-density heuristics) — decide on probe evidence, not upfront.
4. ⏳ **New-menu gate dims** for Phase 4 (recommend: same 5 frozen dims, food-scoped, same as the vertical six).
5. ✅ **Fixture-count decisions (user, 2026-07-13):** Bistro gates 24 food dishes (Pizzas 15 + Ensaladas 4 + Pastas 5); the 2-item "Agrega a tu pasta o ensalada" block is IGNORED (tolerated via section_headers + the ±3 items band). Polloteria: Malteadas stays inside Bebidas (drinks, ungated) → 40 food + 15 drink. GuestHouse: 48 food-scope items; Seafood Plateau = one item, section_title null. All three corrected drafts verified against manual counts 2026-07-13.
6. ✅ **Horizontal Phase-1/Phase-2 boundary (user, 2026-07-14):** printed size variants that Phase 1 temporarily emits as multiple item representations (currently Grande/Jumbo and combined+split Alitas) remain owned by Phase 2's mandatory Shape-A fold and do not by themselves block Phase 1. This is NOT permission to ship duplicate cards: Phase 2 must fold them before release. Unprinted/invented items remain release-blocking in Phase 1. Because eval 067 proved the item-count band can pass while inventions survive, Phase-1 probes require raw-dump/photo audit for inventions in addition to the numeric scorer.
7. ✅ **Dense horizontal support remains mandatory (user, 2026-07-15):** closing this roadmap by declaring horizontal menus unsupported is not an option; most horizontal menus Santiago has observed are dense. Santiago is open to evidence from both upstream nested-tile density reduction and a genuinely spatial/card-ownership verifier, but this is not live-cost approval—the probes still require separate designs, checkpoints, estimates, and explicit approvals. Guided capture of 3–4 overlapping close-up photos is also a research candidate, not a ruling: it may improve text scale but must prove full-column/edge coverage without relying on users to crop correctly.
8. ✅ **Phase-1 re-sequencing after external research (user, 2026-07-17):** the external deep-research report (section below, eval 070) is adopted as directional evidence. New Phase-1 order: **Eval 071 = harness-only spatial co-location replay FIRST** (layout-OCR boxes vs archived Eval-068/069 candidates — the one mechanism not yet tried), **then Eval 072 = nested tile-3 density probe** (the eval-069 recommendation, spec updated to ≥25% sub-crop overlap). Sequencing is ratified; live-cost approvals remain separate per probe at spec time. Guided multi-photo capture (report hypothesis C) and the full hybrid layout-first pipeline (hypothesis D) are deferred post-release; the model bake-off gains layout-first candidates. Mistral OCR's role in Eval 071 is coordinates provider, NOT extractor — this supersedes the never-concluded 2026-06-23 extractor-swap evaluation's framing.
28. ✅ **Guest House "Enhancements" section = `food` (user ORACLE-CHANGE, 2026-07-21):** the 12 priced Enhancements (Foie Gras $26, King Crab $48, Half Lobster Tail, Osetra Caviar … + sauces Chimichurri/Béarnaise/GH Steak Sauce $5) are re-adjudicated from `"other"` to `"food"` in BOTH the truth (`GuestHouseMenu.png.draft.json`) and the fixture (`guest-house.expected.json` `category_expectations` FOIE GRAS → food). Rationale: it is a real premium add-on FOOD section, not a condiment panel. Context (eval 092): the v1.2 filter (ruling 12) drops `category:"other"` on the tile path; these survived a run only because GPT labeled them food; the truth previously said other → contradiction. No structural discriminator separates them from the polloteria condiment "other" class ruling 12 correctly kills (BOTH are priced lines under their own section — a price rule would regress polloteria's eval-076 salsas); the difference is purely semantic, so it is resolved at the oracle. Residual risk: a future run mislabels them `other` → they'd be dropped; watched across the H1.3 ×3 gate. Landed + self-check ALL DIMS PASS (`07b955a`).
27. ✅ **Horizontal is LAUNCH SCOPE — rotation included; roadmap restructured into H1–H4 (user, 2026-07-20):** horizontal menus ship WITH launch (not deferrable). Users photograph wide menus with the CAMERA by turning the phone → captures arrive PORTRAIT-shaped with the menu rotated 90° (sideways) → hiding gallery import does NOT avoid them. Runtime path: camera capture (sideways) → **rotate upright** (was "Phase 3", now H2 — content-based + direction-aware per rulings 22–23, NOW launch-critical, no longer deferred) → **tile** (must be recall-safe on spacious menus — the eval-091 regression) → extract. Clarification recorded: **1f = the dense-vs-not detector, PURELY a cost optimization** (a non-dense wide menu gets the ~$0.05 whole read instead of ~$0.24 tiling); it stays PARKED post-release (ruling 24 tile-all). Restructured launch plan: **H1** recall-safe tiling (fix eval-091), **H2** rotation, **H3** production wiring + e2e, **H4** combined 9-menu gate + device. Phase-2 size-variant conventions + ruling-20 bake-off also PARKED post-launch. Supersedes the earlier framing where rotation followed 1f.
26. ✅ **Printed upsells acceptable as an OPTION or as DESCRIPTION (user, 2026-07-19):** a printed priced upsell ("Agrega queso a tus papas por $10", one per S&H card) is acceptable whether the model emits it as an item OPTION or leaves it in the item's DESCRIPTION — neither form fails the gate (user: "either if it's added as an option or left as description text, whichever is fine"). Generalizes ruling 16. The DESCRIPTION form already passes (description is ungated). The OPTION form is fixed by making `tolerated_option_names` matching SUBSTRING/contains (an option whose normalized name CONTAINS a tolerated token is tolerated) instead of exact equality — so the upsell is tolerated regardless of phrasing ("Queso" in evals 082/085 vs the verbatim "Agrega queso a tus papas" in eval 090 r2). Safe: "queso" appears as an option-name substring only in this upsell class, and expected options are consumed/matched BEFORE the FP tolerance check, so genuine expected queso options are unaffected. Scorer-only change (`eval-extraction.ts`), TDD'd; no production/pipeline change.
25. ✅ **Petrica-class twins tolerated for release (user, 2026-07-19):** the same-price corrupted-name twin class — an UNANCHORED final item whose name sits within the existence tier's loose allowance (≤len/3 edits) of a printed line that a better-matching FINAL sibling owns at the SAME price (eval 089b r1: `Petrica $252` beside verified `OSTRICA $252`) — is TOLERATED for release (observed 1/6 audited tile-path gate runs; scorer-invisible by design). Ruling-6 audits now RECORD this class without blocking; every other unprinted/invented class remains release-blocking (this narrows the 2026-07-14 duplication ruling for exactly this shape). The candidate fix — sibling-aware existence twin rule (drop unanchored bestSim≥0.75 item whose best line is strictly-better-owned by a same-price sibling) + its $0 replay-sweep validation — is PARKED POST-RELEASE (master roadmap list). B1' bistro-through-tiles judged CLOSED under this ruling: eval-089-r1 re-scored ALL PASS + audit clean; eval089b-r1 parked-class only; eval089b-r2 clean.
24. ✅ **Tile-all-horizontal — dense discrimination on wide menus ABANDONED for release (user, 2026-07-18):** any landscape page (client `photo_dims` width>height) returns `needs_crops` deterministically with NO model call; `LANDSCAPE_PROMPT_SUFFIX` deleted (both wordings dead); the `photo_dims` plumbing (ruling 21) is retained as the routing input. Evidence: evals 086–088 killed every cheap single-signal detector on the same menu pair — model self-report brackets without separating (086/087), items/MP fails on dense under-reads (087 analysis), physical text size interleaves (088: bistro 10.9px BETWEEN polloteria 9.2 and nikkori 12.2; p75 inverted). User rationale: discrimination is a cost nice-to-have, not release-critical — prioritize release speed. Supersedes ruling 21's suffix mechanism and the same-day keep-discriminating cost ruling. Consequences: dense wide scans get ~$0.05 cheaper (the discarded whole read is skipped); simple wide menus pay ~$0.24 vs ~$0.05 (rare — most observed wide menus are dense); bistro/guest-house non-regression probes now run THROUGH TILES (B1'/B3); **post-release work item: wide-menu dense discrimination (cost optimization)** — parked in the master roadmap's POST-RELEASE list; portrait path untouched (model self-report stays).
23. ✅ **Capture-flow constraints (user, 2026-07-18):** the app's capture screen produces PORTRAIT photos ALWAYS — users flip the phone to fill the frame with a horizontal menu, so horizontal menus reach the pipeline as sideways-text-inside-portrait photos. Consequences: the camera core flow (snap → goals → sorted results) requires 1f AND Phase 3 together; build order forced 1f → Phase 3 (Phase 3's output is an upright wide image that lands on 1f's path; 1f alone already serves gallery-imported wide photos, today's only production source of landscape uploads). Santiago is CONSIDERING hiding the gallery-import feature pre-release to shrink scope (NOT decided — affects entry points only, no pipeline change). Everything must work on both iPhone and Android (the approved design uses only client-known dims + existing cross-platform expo APIs — compliant).
22. ✅ **Phase-3 ground rules (user, 2026-07-18):** production currently has NO sideways-menu handling (Phase 3 unstarted — a sideways-menu photo today returns garbage). (a) Orientation detection must be CONTENT-BASED from the captured image — NEVER device-sensor-based (user: a phone held sideways over a vertical menu would mistrigger; planner: gravity sensors are also blind when the phone is flat over a table, the most common menu-photo posture). (b) Rotation must be DIRECTION-AWARE — a sideways menu may need 90° CW, 90° CCW, or 180°; never hardcode one rotation. Candidate mechanisms to brainstorm/probe at Phase-3 kickoff (NOT now): extraction-call layout signal reporting text direction (a needs_rotation round-trip à la needs_crops) vs deterministic try-rotations-scored-by-cheap-OCR; the capture-time user prompt parked under ruling 21 is the fallback if content-based detection proves unreliable. This supersedes the Phase-3 section's older "EXIF orientation / aspect heuristic" candidate list — EXIF/aspect cannot see a sideways menu INSIDE a portrait photo.
21. ✅ **1f detector-trigger design = client-declared dims + landscape-scoped suffix, with FP policy (user, 2026-07-18):** stage:"extract" gains optional `photo_dims: {width,height}[]` parallel to `photos` (the client sends the same orientation-correct values it already trusts to cut tile geometry; identical on iPhone/Android; `ocr_photos` validation precedent). The edge appends a landscape-only (width>height) density-assessment suffix to that page's phase-1 call — the TILE/PAGE_PROMPT_SUFFIX scoping pattern; portrait pages and absent-field requests get a byte-identical prompt (fail-open; old builds unaffected; vertical safety by construction). REJECTED: edge image-header parsing (EXIF-rotated camera JPEGs swap raw stored dims; heavy binary code to recover information the client owns); deterministic force-tile-on-landscape as primary (bistro would pay ~5× every scan) — retained ONLY as the fallback lever if probes show the suffix cannot fire reliably; capture-time user prompt (answers orientation, not density; adds a tap to every core-flow scan) — parked as a Phase-3 fallback. Predeclared probe gates: polloteria `dense` fires 3/3 REQUIRED; bistro stays `dense=false` 3/3 REQUIRED (ONE suffix-rewording iteration ≈$0.15 authorized-in-principle before escalating back to Santiago); guest-house RECORDED ONLY — either verdict acceptable (user: "cool if dense, wouldn't mind if not"), its non-regression probe gates whichever path production routes it to. Live launches still need fresh cost approval (the prior-era ~$0.90 approval is STALE).
20. ✅ **Scoped extractor bake-off probe between Phase 1 and Phase 2 (user, 2026-07-18):** the never-concluded 2026-06-23 Mistral-as-extractor question is revived as a HARNESS-ONLY probe inserted after the Phase-1 exit (1f done) and before Phase-2 kickoff. Rationale: no extractor comparison has ever run under the CURRENT architecture (the Stage-1 benchmark predates tiles/verify/colocation), and the cached full-photo Mistral OCR reads polloteria's names/prices essentially perfectly. Scope: same fixtures/scorer/ruling-6 audits; candidates ≥ {current GPT-4o stack, Mistral-OCR→LLM-grouping layout-first hybrid}; cached-OCR $0 work first, live cost estimated at spec time with fresh approval. Output = EVIDENCE for the post-#5 full bake-off (Gemini 3.x, PaddleOCR-VL per eval 080), NOT a production migration decision — unless results justify escalating, which is Santiago's call.
18. ✅ **Section-title punctuation fold (user, 2026-07-18):** tile-path deterministic rule — section titles that differ only by punctuation (`Pa Compartir` vs `Pa'Compartir`, eval-082 apostrophe variance across tiles) fold to one spelling (majority, tie → longer). Production postprocess, TDD'd.
17. ✅ **Ensaladas Shape-A options stay Phase-2-owned (user, 2026-07-18):** the natively-extracted `G (300gr)` options (eval 082 — ruling 1's exact shape, printed prices verified) are TOLERATED now via unchecked pins; promotion to REQUIRED pins + the mandatory `Ensaladas` sections restore happen together at Phase-2 close.
16. ✅ **Printed upsell options are legitimate (user, 2026-07-18):** prose upsells the model converts to options (`Agrega queso a tus papas por $10` → `Queso`@10, eval 082 ×7 S&H cards) are accepted extras — "good it was able to read and turn into an extra". Mechanism: fixture-level `tolerated_option_names` scorer field (`tolerated_categories` precedent) — an FP item stops counting when every option it carries is tolerated. Standing cost approval for the test runs (~$0.24/run, ~$0.73 per ×3 attempt) granted same day.
15. ✅ **Co-location evidence source v1.3 + harness cutter fix (user, 2026-07-18):** approved on eval-079 evidence (3-dump replays, OCR deterministic ×3). The stage's evidence source becomes ONE Mistral OCR call on a 2048/q0.95 JPEG copy of the original photo, carried as optional `ocr_photos` in extract-pages (client reuses the ticket-#3 `compressImage` transform; absent → fail-open skip); the per-tile OCR path is deleted (tile-edge fragment blocks spuriously anchor inventions; mega-block tiles starve regions → real-card false drops; also never worked on-device — bare-base64 tiles). Includes fixing the harness sips cutter (flush-bottom x=0 crops silently no-op → every harness "tile 3" since eval 061 was the FULL photo; production client unaffected; closed nikkori gate DECISIONS stand — device runs used real tiles — but Phase 4 runs on the fixed cutter). Matcher/polarity/v1.1/v1.2 unchanged.
13. ✅ **Eval-078 photo adjudication (user, 2026-07-17):** the printed Ensaladas section has exactly 3 cards (Ensalada Boneless M/G, Aguacate Chicken M/G, Ensalada Pollotera M/G) — `Ensalada de Pollo` is NOT printed; Pollo Kids prints `Nuggets (200gr) $89` and `Nuggets & Waffles (200gr) $92` — `Nuggets de Pollo` is description text only. Both eval-078 survivors are confirmed inventions; the 0-audited-greens verdict is final.
14. ✅ **Scorer misread-name tolerance (user, 2026-07-17):** a minor misread of a real card's name (`Papa Sazonada` for printed `Papas Sazonadas`) is tolerable and must not fail the categories/grams expectation lookup — the scorer's expectation matching gains a small edit tolerance for this class (scorer/fixture-side change only, TDD'd; exact mechanism = next planner's design; no production pipeline change under this ruling). Invented names remain release-blocking — tolerance applies to expectation LOOKUP, never to the ruling-6 invention audit.
12. ✅ **Condiment-panel filter v1.2 (user, 2026-07-17):** tile path drops `category:"other"` items beside the per-tile drink filter — eval-076 evidence (9 salsa/aderezo items caused all 3 failed dims) + archive survey (42 'other' in 4,037 items ever, zero real dishes). Portrait path untouched.
11. ✅ **Existence tier v1.1 + per-menu readability gate (user, 2026-07-17):** spec amendment approved on eval-075 evidence — unanchored unverifiable items whose name matches no printed line even loosely (best-line similarity <0.75) are dropped as inventions, but ONLY on menus where the OCR proves it reads the names (≥50% of non-drink items anchored; nikkori-class menus keep the tier inert). Perfect separation on Polloteria (misreads 1.00 vs inventions ≤0.67); the user-corrected draft exposed 2 additional stable inventions (Sour Cream Cheese Fries, Ensalada de Pollo a la Plancha) that count bands had hidden since the start.
10. ✅ **Dedos De Queso adjudication + production spec green-light (user, 2026-07-17):** printed card reads `Dedos De Queso (200gr) $199` — GPT-4o misread $119; Mistral OCR read the print correctly. First reader-disagreement data point favors OCR, but field repair stays v2 (one data point). Production co-location stage spec APPROVED to write: `docs/superpowers/specs/2026-07-17-production-colocation-stage-design.md` (tile path only, per-tile OCR, sibling-aware drop-only polarity v1, fail-open, advisory completeness log; nikkori false-deletion replay mandatory before any live run).
9. ✅ **3-way co-location semantics + Alitas oracle correction (user, 2026-07-17):** the Eval-071 checker adopts three verdicts — VERIFIED (an anchor block prints every claimed field → keep), CONTRADICTED (no anchor verifies AND a PRICED anchor — a block printing a $-price — prints a different value of a claimed field type → fake, reject; prose anchors can never contradict), UNVERIFIABLE (nothing verifies, nothing contradicts → KEEP the card, flag the unprinted field, never delete). ORACLE-CHANGE: `alitas-phase2-b` expectation corrected to UNVERIFIABLE — Santiago visually confirmed the printed Alitas card reads `Alitas 6 PZ $129 / 12PZ $169 / 20PZ $269` with no grams; the candidate's `125gr/315gr/525gr` are model-added. Fakes must land CONTRADICTED; UNVERIFIABLE is never a deletion.

## External research review (eval 070, $0 — 2026-07-17)

Santiago commissioned an external LLM deep-research report on dense horizontal-menu extraction (delivered 2026-07-15; archived here as `2026-07-15-external-research-dense-menus.md` — read it in full before designing Phase-1 probes). Planner assessment and what it changes:

- **The failure class is known and open.** Cross-card field recombination = DoorDash's "item↔attribute linkage" failure / relational VLM hallucination, caused by tile-based image encoding fragmenting dense regions — an architectural property, not a prompt defect. No published system guarantees zero recombination; production systems manage it with layout-first decomposition + spatial grounding/provenance.
- **Independent convergence:** the report's cheapest decisive experiment is exactly eval 069's recommended nested tile-3 probe, derived without ledger access. Its "count-tolerance masking" warning is our eval-067 proof; its "never auto-delete, flag instead" safety rule is our keep-bias discipline; its verdict that semantic plausibility cannot establish distinctness validates rejecting verifier v1–v3/batched/paired (evals 063–069) as correct process-of-elimination, not churn.
- **The one new mechanism: spatial co-location against a dedicated layout parser's boxes.** Require a candidate's name+price+grams to co-locate inside ONE region box from a real OCR/layout pass (VLM self-reported boxes are unreliable on dense small text). This operates on the actual root-cause variable — spatial card ownership — where every prior verifier operated on semantics.
- **Also adopted:** pixel stitching of repeated menu text is a documented duplicate generator → any future guided-capture pilot must be region-based with structured-data-level merge; GPT-4o is a legacy model with weak grounding → the bake-off must include layout-first candidates (Mistral OCR + LLM grouping, Gemini 3.x, PaddleOCR-VL), not only pure-VLM swaps.
- **Caveats:** the report's vendor pricing/benchmarks/launch dates are unverified secondary claims — re-verify official pricing at Eval-071 spec time before budgeting; the architectural argument stands on our own ledger evidence regardless.

## 🅿 PRIORITY ZERO — GPT-4o→Mistral-OCR extraction migration (ruling 29, 2026-07-22) ← ACTIVE

**Goal:** Mistral OCR (`document_annotation_format` = our menu schema) replaces GPT-4o Vision as the Stage-1 extractor, reaching eval093/H1.3 quality (5 dims ×3 + ruling-6 audit) on the release-critical fixtures. GPT-4o stays for Stage-2 enrichment only.

**Why now (eval 095 evidence, $0.045):** one Mistral annotation call, whole upright image, NO tiling → items PASS raw ×3 on all 3 wide menus (full recall), ~0 inventions, PERFECTLY deterministic ×3, correct names, price recall ≥ GPT. Mistral transcribes rather than invents (structural anti-hallucination) and is cheaper + deterministic. The only gap is cleanup of Mistral's own regular artifact shape.

**The gap to close (measured):** Mistral's structured output carries (a) a **self-echo weight-option** per item (`TostiBoneless → "Boneless (300gr)"`) — inflates options + hides the weight from `parseItemGrams`; (b) **Shape-A `M/G` splits** as sibling items/options (the same Phase-2 convention gap open for GPT too — ruling 1); (c) **section-title spacing/spelling** (`PolloKids`→`Pollo Kids`). The incumbent `postprocessItems` is tuned to GPT's artifacts, does NOT clean these, and even REGRESSES polloteria (promotes the self-echoes into phantom items).

**Sub-steps (cheap-first; each gated before the next):**
- [x] **M0 — B1 feasibility probe** — CLOSED 2026-07-22 (eval 095). Script `scripts/probe-bakeoff-mistral-b1.ts` (`f0fd0f8`); 9 dumps cached `~/Downloads/MenusTesting/*.mistral-b1-r*.{raw,dump}.json`.
- [x] **M1 — Mistral-specific deterministic cleanup layer — OFFLINE GATE MET 2026-07-22 ($0):** `scripts/mistral-cleanup.ts` (final HEAD `7f8446c`, 10/10 tests). Chain: `dropDrinkSections` (menu-generic — drops drinks + any section holding a drink item, via the model's own `drink` labels) → `dropOtherCategoryItems` (ruling 12) → `dropSelfEchoWeightOptions` (drop null-price options that echo the item name OR carry a bare `(NNNgr)` weight; **SUM** the folded weights into item grams = combo total food weight) → `normalizeSectionTitle` (camelCase split). **Offline re-score of the 9 cached B1 dumps: polloteria ALL-5 ×3 (release-critical dense, ruling-6 clean), bistro 4/5 ×3 (only options — the E item, pending M2), GH recall 48/48 + zero-invention ×3 (options/section/grams Phase-2-deferred = eval093 GH scope).** Fixture re-fits (Santiago ORACLE): B Megacharola unchecked pin, C two Paletas pins→`"Paletas Heladas"`, D Megacharola grams 1200→1800 (fixture+draft; Santiago's draft had undercounted it). **KEY LESSON for M3 wiring:** the cleanup MUST NOT call `parseItemGrams` — Mistral puts weights in options (folded/summed), and parseItemGrams grabs the first weight from a combo's DESCRIPTION and clobbers the correct sum (Megacharola→1200 instead of 1800). Grams come from the folded options, full stop.
- [ ] **M2 — scored ×3 live gate** (needs fresh cost approval, ~$0.05–0.15): Mistral-B1 + M1 cleanup vs the 3 wide fixtures, 5 dims + ruling-6 audit, ×3. Determinism means ×3 may be cheap/near-redundant — but keep the ruling-6 audit per run (ruling 6).
- [ ] **M3 — edge wiring + architecture simplification:** swap the extractor in `extract.ts`/`runPagedExtraction` (Mistral annotation call replaces the GPT-vision call); RE-EVALUATE removing the colocation stage (ruling 15 — likely moot) and the tile-all path (ruling 24 / H1 — likely moot; Mistral reads the whole image). Portrait/vertical byte-identical safety re-established for whatever remains. GPT-4o enrichment untouched.
- [ ] **M4 — re-prove ALL 9 menus** (the biggest scope risk): Mistral is now the extractor for EVERY menu, so the 6 CLOSED vertical gates + the 3 horizontal must ALL pass under Mistral. $0 recall-ceiling checks first (OCR caches exist for all 9 — `*.mistral-ocr-2048q95.json`), then a combined scored gate. The incumbent GPT gate closures do NOT transfer for free.

**Exit:** Mistral-OCR extraction matches GPT-4o eval093 quality across the release fixtures; colocation/tiling simplified or removed; pipeline diagram updated. THEN resume H2 rotation → H3 wiring → H4.

**Do NOT:** touch production edge code before M1's $0 offline gate proves the cleanup layer works on the cached dumps; assume the GPT postprocess/colocation transfers to Mistral; drop the ruling-6 invention audit because the raw scorer passes.

## 🚀 LAUNCH PLAN — horizontal H1–H4 (ruling 27, 2026-07-20) — PARKED behind Priority Zero (H1 stays CLOSED)

The end-to-end camera path a wide menu must survive:
```
camera photo of a wide menu  →  arrives SIDEWAYS (portrait-shaped, menu rotated 90°)
   → [rotate upright, correct direction]   ← H2 (rotation) — NOT built
   → upright wide image (width > height)
   → tile it — and lose NO dishes           ← H1 (safe tiling) — eval-091 regression open
   → extract (proven clean on dense: eval 085)
   [1f dense-check would only slot here to save cost — PARKED post-release]
```

### H1 — Recall-safe tiling on EVERY wide menu (fix the eval-091 regression)
Tiling currently LOSES dishes on spacious menus (Guest House eval 091: whole 12-item Enhancements column vanished + existence tier deleted 3 real Sides). Tiling must never drop a real dish before we ship tile-all.
- [x] **H1.1 — $0 diagnostic (DONE 2026-07-21, ledger "H1.1 diagnostic + H1.2a"):** (a) Enhancements loss = TILE GEOMETRY — the 12-item 2-column block at norm x[0.40,0.67] y[0.75,0.87] straddles the x=.40/.60 seam → clipped in both containing tiles (t2 right col, t3 left col) → `TILE_PROMPT_SUFFIX` skips all 12; fixable via more horizontal overlap OR whole+tiled union; do NOT touch the suffix. (b) 3-Sides false-deletion = `bestLineSim` denominator, NOT OCR quality — names read verbatim but trapped in merged 2-column priced rows; 2/5 tokens = 0.40 < 0.75.
- [x] **H1.2 — implement the fix (DONE 2026-07-21).** **H1.2a existence-tier guard `0d446da`:** `splitPricedRow` inside `bestLineSim` (≥2-price blocks split into per-dish segments; prose/single-price untouched → description-mint guard intact); verified on pre-colocation dumps (all known inventions still dropped, the 3 Sides KEPT). **H1.2b recall fix `f9b2029` — landscape-only `gridCropRects` 0.65/0.35 overlap** (eval 092: Approach A beat whole-read union, which injected dense phantoms → rejected). Plus null-grams twin-fold `07b955a` (kills the Butcher steak overlap twins) + ruling 28 (Enhancements=food) + GH `items_over_tolerance:6` + Osetra grams 30→3 `2bf8680`. Portrait/nikkori byte-identical (geometry scoped to w>h).
- [x] **H1.3 — GATE PASSED 2026-07-21 (eval 093, ~$2.6, scoped per ruling 27):** all 3 wide menus ×3 through the new-geometry tile path. **Polloteria (dense, release-critical): ALL 5 DIMS PASS ×3** + ruling-6 clean (40 real full recall + 2 Paletas, zero inventions, no crush). **Bistro: ALL 5 DIMS PASS ×3.** **Guest House: recall PASS ×3 — full 48/48 incl. all 12 Enhancements + 3 Sides, zero inventions**; options/section_context red by design (Shape-A splits, Phase-2-deferred). Scorer tolerances added (TDD + adversarially verified): section-title punctuation (`ae0cc03`) + spelling fuzzy-match (`1607ddd`, for the new-geometry `Hamburguesas`→`Hamburgers` misread; rejects distinct sections, GH splits still flag spurious).
**Exit:** ✅ MET — tiling adds detail without ever losing dishes, on dense AND spacious wide menus; release-critical dense menu fully green ×3.

### H2 — Rotation (the camera core flow)
Camera photos of wide menus arrive sideways and read as garbage today. Detect + rotate upright. Rulings 22–23: CONTENT-BASED detection (never device sensors), DIRECTION-AWARE (90CW/90CCW/180, never hardcoded).
- [x] **H2.1 — mechanism FOUND (eval 094, 2026-07-22; $0.048):** OCR/on-device BLOCK-GEOMETRY is a clean single-shot, direction-aware detector (12/12 on 4 real sideways captures). `wide-frac` (fraction of text blocks wider-than-tall) gives the AXIS (sideways≈0.0 vs horizontal≈1.0, huge margin); `sign(corr(readingOrder, blockCenterX))` gives POLARITY (+=upright, −=upside-down); `sign(corr(readingOrder, blockCenterY))` gives the sideways turn direction. Mistral OCR is orientation-ROBUST for text (so "reads well" can't verify orientation) but degrades on PRICES sideways → rotation stays required. Santiago constraint: a fixed rotation is unsafe (naive `sips -r 90` produced upside-down bistro) → direction-aware + re-verify, ≤2 tries never 4. The signal is pure block geometry → **on-device iOS Vision / Android ML Kit can deliver it at $0/scan** (finding transfers; likely the production path). Script `scripts/probe-rotation-h2.ts` (`1ef41fe`); caches `~/Downloads/MenusTesting/MenuRotationTesting/*.mistral-ocr.json`. NOTE: the executor's assumed rotation degrees were photo-shown INVERTED (true-upright = the `rFlip` set) — pin sign→degrees empirically at build. PARKED behind Priority Zero.
- [ ] **H2.2 — build rotation** (client-side, cross-platform iPhone+Android) + rotated fixtures (rotated copies of the 3 wide menus; one oracle serves the upright + rotated twin).
- [ ] **H2.3 — GATE:** each rotated menu scores == its upright twin; the 6 vertical/portrait menus NEVER rotate (false-positive discipline).
**Exit:** a sideways capture reads as well as an upright one; zero false rotations on portrait menus.

### H3 — Production wiring + end-to-end
- [ ] **H3.1** — client sends the signals the edge needs (photo dims + rotation result); edge `isValidPhotoDims` validation + routing; REMOVE the "don't wire dims" guard and enable the tile trigger.
- [ ] **H3.2** — end-to-end device test: photograph a wide menu (one dense, one spacious) with the camera → rotated → tiled → correct extraction on the phone.
**Exit:** a real device scan of a horizontal menu returns correct results.

### H4 — Combined exit gate + device verification
- [ ] **H4.1** — 3/3 consecutive all-green across ALL 9 menus (6 vertical + 3 horizontal) × frozen dims + rotation assertions (`eval-027-live.ts` grows the 3 new fixtures).
- [ ] **H4.2** — device scans (dense landscape + a rotated capture); offline-score via `score-dump.ts`. Close-out: ledger, pipeline diagram, master-roadmap #4 tick, memory.
**Exit:** horizontal shipped.

**Sequencing:** H1.1 diagnostic → H1 fix+gate → H2 rotation → H3 wiring → H4. (H1 must land before H2's gate — rotation feeds into tiling, so tiling has to be correct first. H2.1's design brainstorm can run in parallel since it's independent code.)

**PARKED post-release:** 1f dense-detection cost-optimization (ruling 24); Phase-2 size-variant conventions (Shape A — Seafood Plateau / Ensaladas splits, currently tolerated); ruling-20 extractor bake-off.

## Phases — historical trail (structure SUPERSEDED by the Launch Plan above; kept for detail/evidence)

### Phase 0 — Fixtures from the corrected drafts ($0) ← NEXT
Build `scripts/fixtures/{bistro,polloteria,guest-house}.expected.json` from the user-corrected `*.draft.json` + manual counts. Include: food/drink split (drinks parked à la nikkori), `sections` + `section_expectations`, `items_with_options` (incl. rulings 1–2 once made), `category_expectations`, `grams_expectations` (Polloteria's printed grams are rich), cropped-price bebidas simply omitted from gated checks (drinks ungated). Validate each fixture offline against its corrected draft via `score-dump.ts` — the corrected draft should score ALL PASS against its own fixture (self-consistency check); the UNCORRECTED original extraction (re-runnable) documents the measured gap. Scorer must not need code changes; if a fixture shape lacks scorer support, that's a planner review item, TDD'd.
**Exit:** 3 fixtures committed, self-consistent offline, gaps documented in the ledger.

### Phase 1 — Dense-landscape handling — IN PROGRESS (full trail: ledger evals 060–062 + rulings 2026-07-14)
Status 2026-07-14: detector blind spot diagnosed (eval 060: dense=false, crop_direction=none 3/3 — landscape's 768px-shortest-side rescale crushes text; the model never signals). Cure proven: forced 2×2 tiles recover full coverage (4×1col REJECTED — section loss). Hygiene ladder landed (ledger 061-062): tolerated_categories scorer field, dropBannerEchoOptions, nullPriceNoteSections, tile-pass twin fold + truncation drop in merge.ts, misread pin retargets. Residual = recombination phantoms + double-flake twins → **USER RULING 2026-07-14: duplication/invention is trust-critical (release-blocking); the name-verification pass is pulled from the post-release OPTIONAL list into THIS phase (tile path only)**; band stays +5. Verification-pass trail COMPLETE (ledger evals 063–069): semantic name verification v1–v3, batched calls (068), and the paired two-image comparison (069) were ALL rejected by predeclared gates — the deciding variable is spatial card ownership, which no semantic verifier can see. Re-sequenced remaining sub-steps (ruling 8): (1c) ✅ **Eval 071 — spatial co-location replay, harness-only — PASSED 2026-07-17 (14/14 on the replay under ruling 9's 3-way semantics + v3 mention-guard; ledger "Eval 071 CLOSE"):** one layout-OCR pass (Mistral OCR; ~cents — verify current official pricing at spec time) on `PolloteriaMenu.png` → region/word boxes; a deterministic geometry check that each archived Eval-068/069 candidate's name+price+grams co-locate in ONE region box; adjacency tolerance iterates offline at $0. Predeclared gate: 5/5 known fakes rejected AND 3/3 controls kept → co-location becomes the Phase-1 release gate; 4/5 with all controls kept → advisory flag only; any control rejected → tighten or reject the architecture. Known fail-closed risk: Spanish accents/stylized fonts may break candidate↔token matching and falsely reject real dishes — polarity stays flag-don't-delete until the gate passes. (1d) **Eval 072 — nested tile-3 density probe** (eval-069 recommendation, ~$0.72 hard cap ×3, stop on first failure): freeze the three clean Eval-068 parent tiles, replace only tile 3 with a nested 2×2 at **≥25% overlap** (report: our parent 2×2 overlap is far below documented mosaicing practice); targets recall (the missing Ensalada card) and generates fewer fakes upstream — complementary to 1c's precision gate. (1e) ✅ **polloteria ×3 confirmation through the production path — CLOSED 2026-07-18 (eval 085: 3/3 scorer + 3/3 ruling-6 audits, detector forced)**; (1f) detector trigger — RESOLVED 2026-07-18 per ruling 24 (supersedes ruling 21's suffix; header parsing remains rejected on EXIF grounds): deterministic tile-all-horizontal — landscape pages (client `photo_dims` w>h) route to `needs_crops` with no model call. Probes: $0 probe-detector check ×3 wide menus (landscape now short-circuits with zero API calls) → B1' bistro-THROUGH-TILES quality (~$0.24/run — bistro never rode the tile path; the one real risk) → B2 polloteria e2e trigger-live ×3 (~$0.87) → B3 guest-house (~$0.29) → production wiring → the Phase-1 exit criteria below.
Original phase design (kept for reference):
1. **Detector diagnosis (~$0.15):** Polloteria ×3 phase-1 — is dense=false stable? Does `image_layout.crop_direction` say anything useful? Ledger the base rate before touching anything (lesson #7: instrument before theorizing).
2. **Detector fix:** per ruling 3 — candidate levers in cost order: aspect-aware deterministic assist in the detector consumer (no model change); a single mode/aspect-scoped P1 layout-assessment sentence (probe-driven, one variable at a time, watch the 5 vertical menus' detector false-positive rate on archived evidence first, live spot-probes only if the change could plausibly reach them).
3. **Landscape tile geometry:** extend `gridCropRects(w, h)` to return a landscape grid (start 3×2, same overlap philosophy) when w>h; portrait path byte-identical (unit-pinned). A/B on Polloteria (~$0.30/round) oracle-scored: 2×2 vs 3×2 vs no-tiling baseline. Tile fidelity rules stay (PNG from originals).
4. Re-check Polloteria names/counts through the winning path — quantifies how much of the misread class Phase 1 already killed.
**Exit:** Polloteria passes its scoped dims ×3 consecutive probes through the production path (detector fires, tiles win) AND raw-dump/photo audit finds zero unprinted/invented items; printed multi-representation size variants remain Phase-2-owned per ruling 6. Bistro + GuestHouse unregressed (offline + 1 live probe each); vertical portrait behavior unit-pinned unchanged.

### Phase 2 — Size-variant extraction convention (P1 + filter revision, full probe discipline)
Blocked on: ruling 1, and Phase 1 (variant items on Polloteria must be read through working dense handling before measuring). Work: revise the F2 "sizes are NOT options" scope — printed size variants WITH their own printed price/weight become options (macro-relevant: M 150gr vs G 300gr doubles macros); copa/botella-style format-only variants (no per-size print) stay excluded. Touches P1 (one sentence at a time, non-local-side-effect discipline) + `filterServingFormatOptions` (TDD; the wine-format tests must keep passing). Validate: offline vs ALL archived vertical dumps ($0) → sensitive-menu live probes (brasero/brasero-two are the serving-format-filter menus) → 3-menu landscape probes.
**Exit:** all size-variant targets in the 3 new fixtures extracted per the ruled shape ×3 probes; zero new option FPs on the vertical archive + probed menus.
**MANDATORY RESTORE — Phase-2 evaluation inputs (temporary Phase-1 tolerances; each MUST be restored/promoted and pass before Phase 2 closes — these ARE the Phase-2 dev test set):**
- **Polloteria (user-approved 2026-07-14):** `polloteria.expected.json` moved `"Ensaladas"` from required `sections` to tolerated `section_headers` because its size-variant card titles (Phase 2's exact subject) mutate per run and killed Phase 1's section_context 3/3. Restore "Ensaladas" into `sections`; promote the Ensaladas Shape-A `G (300gr)` options from unchecked to REQUIRED pins (ruling 17).
- **Guest House (user-approved 2026-07-21, H1.3 gate decoupling — ruling 27 defer-to-Phase-2 + eval 092):** GH is a bake-off/record asset, NOT release-critical; its Shape-A size-variant + option quality is DEFERRED to Phase 2 so H1 (recall-safe tiling) can close on RECALL + zero-inventions. Temporary Phase-1 tolerances now in `guest-house.expected.json`: **`items_over_tolerance: 6`** (absorbs the +4 Shape-A split children so the items/recall dim passes — Seafood Plateau `FOR TWO`/`FOR THREE OR MORE`, Filet `7oz`/`10oz`). STILL FAILING by design (deferred, NOT tolerated-to-green): **section_context** (the 2 split-parent pseudo-sections `SEAFOOD PLATEAU*` + `PARMESAN-CRUSTED FILET* 7oz / 10oz` emitted as sections) and **options** (2 FP carriers — `HALF MAINE LOBSTER TAIL` chilled/grilled = a real printed prep choice; `MEXICAN WHITE SHRIMP` `$7 EA` = per-unit pricing, the PER_UNIT_NOTE family per ruling 1). Phase-2 work: fold the 4 splits to Shape A (Seafood Plateau FOR TWO 150 / FOR THREE+ 280; Filet 7oz 65 / 10oz 85 — exactly ruling 1's listed cases), which removes the pseudo-sections + the item over-count → then TIGHTEN `items_over_tolerance` back to 3 and require section_context/options green. Non-tolerance oracle fix already landed (not deferred): OSETRA CAVIAR grams `30 → 3` (Santiago photo-adjudicated 2026-07-21: card prints "Osetra Caviar (3g) 20"; draft + fixture corrected).
Phase 2 CANNOT close until BOTH menus' tolerances are restored/promoted and pass.

### Phase 3 — Rotation arm (the original #4 scenario)
Create rotated copies (`sips -r 90`) of the 3 landscape photos = portrait files with sideways text (the expected real capture). Client: detect + rotate upright before phase-1. **GROUND RULES LOCKED (rulings 22–23, 2026-07-18): detection must be CONTENT-BASED from the image (never device sensors); rotation must be DIRECTION-AWARE (90CW/90CCW/180 — never hardcoded); the old candidate list (EXIF orientation, aspect heuristic) is superseded — EXIF/aspect cannot see a sideways menu INSIDE a portrait photo. Candidates to brainstorm at kickoff: extraction-call text-direction signal (needs_rotation round-trip à la needs_crops) vs try-rotations-scored-by-cheap-OCR; capture-time user prompt = fallback. Phase 3 is REQUIRED for the camera core flow (capture screen always yields portrait photos) — it is the immediate next work after 1f closes.** Tiles cut from the ROTATED upright image. Harness mirrors the client rule (photo-input.ts). Assertions: each rotated twin scores == its upright twin (same oracle); the 6 portrait vertical menus NEVER trigger rotation (false-positive discipline); device photos may carry EXIF the PNG twins lack — device verification covers that gap.
**Exit:** rotated twins pass scoped dims ×3; zero rotation false-positives on vertical menus across those runs.

### Phase 4 — Combined exit gate + device verification
`eval-027-live.ts` grows the 3 new fixtures (detector expectations per fixture; rotation assertions per Phase 3). **Gate: 3/3 consecutive all-green on ALL 9 menus × frozen dims + detector + rotation assertions** (~$1.35–1.50/attempt, user approval per attempt). Then device: scan at least Polloteria (dense landscape) + one rotated capture on the iPhone; offline-score via `score-dump.ts` (ticket-#3 protocol). Close-out: ledger, pipeline diagram (landscape/rotation branches), master-roadmap #4 tick, memory.

## Parked hypotheses from prior-art research #2 (eval 080, 2026-07-18) — trigger-gated, no spend before the Phase-1 ×3 gate

- **Document-anchoring probe (olmOCR pattern):** inject the full-photo OCR text (+ positions) into the tile extraction prompt — published hallucination reduction; attacks invention at GENERATION rather than cleanup; we already pay for the OCR call. TRIGGER: the live ×3 on true tile-3 geometry reveals a phantom class the colocation stack cannot catch. Single-variable probe; P1 non-local-side-effect discipline applies.
- **OCR `title` blocks = printed section headers (free, already in our caches):** polloteria's 12 title-typed blocks are exactly the printed headers/panels. TRIGGER: Phase 2 kickoff — $0 exploration vs cached OCR as a section-title verification/repair signal (Ensaladas restore, remapTruncatedSectionTitles hardening).
- **v2 field-repair mechanism note:** localized re-read — re-OCR only the disputed anchor region WITH a white margin (eval-079 pad finding) to adjudicate GPT-vs-OCR field disagreements (Dedos $119/$199 class). TRIGGER: the ×3 probes accumulate more reader-disagreement data points (spec v2 precondition).
- **Per-scan trust score (DoorDash guardrail analog, free):** the colocation verdict distribution (verified fraction + readability) already computed per scan → powers the deferred retake-prompt / confidence-flag features with no new models. TRIGGER: post-release feature work on either.
- **Bake-off additions:** copy-only constrained decoding over the OCR token lattice (report: unclaimed ground — the only mechanism making name-minting IMPOSSIBLE rather than detectable; needs self-hosted model + grammar engine; composes with layout-first); GutenOCR conditional detection (open weights, F1 0.877, zero menu-domain evidence — our Mistral pipeline already provides the primitive at $0.004). Layout-first candidates (below) gain PP-DocLayoutV3 version specifics + the card-detector caveat (menu card ≠ layout class; guarantee only as strong as segmentation).

## Deferred / known limits (do NOT work here)
- **Wide-menu dense discrimination (post-release cost optimization — ruling 24):** the ~$0.20/scan saving on simple wide menus, abandoned for release after evals 086–088. If revisited: OCR-geometry caches exist for all 9 fixture menus (`*.mistral-ocr-2048q95.json`); blocks/MP showed a 1.6× boundary gap on only N=2 dense samples — needs many more wide fixtures before any threshold is trustworthy. Also parked in the master roadmap's POST-RELEASE list.
- Stable name misreads surviving Phase 1 → bake-off + post-release name-verification pass (Polloteria = new bake-off fixture; GuestHouse = the English menu E2 needed).
- Drinks on the new menus (F5): counts parked in fixtures.
- OCR-model alternatives for misreads: bake-off track (after critical-path #5), not here.
- The pixelated third Google-Maps menu the user discarded; user may source another dense-landscape photo later — add as fixture, same flow, no plan change.
- Guided multi-photo capture (research hypothesis C): post-release pilot candidate only; if ever built, region-based capture + structured-data-level merge — NEVER pixel stitching (repeated menu text = documented duplicate generator, i.e. a fake-dish machine).
- Hybrid layout-first pipeline (research hypothesis D: layout parser → bounded tokens → LLM grouping): the end-state architecture candidate — bake-off track, not here. Bake-off adds layout-first candidates (Mistral OCR + grouping, Gemini 3.x, PaddleOCR-VL) beyond E1/E2.

## Reference Block (verbatim essentials)
- Gate runner: `scripts/eval-027-live.ts` (GATE_DIMS = [items, options, section_context, categories, grams]; never plain `--gate`). Scorer: `scripts/eval-extraction.ts` (`scoreMenu`, self-check). Offline: `scripts/score-dump.ts <menu> <dump.json>`. Drafts: `scripts/extract-draft.ts`. Production input mirror: `scripts/photo-input.ts` (passthrough ≤9M b64; 2048/q95 fallback).
- Menus: `/Users/santiagoaguirre/Downloads/MenusTesting/` (6 vertical + BistroMenu.png, PolloteriaMenu.png, GuestHouseMenu.png + corrected `*.draft.json`).
- `OPENAI_API_KEY` in worktree `.env.local` (gpt-4o Tier-1 30k TPM/90k TPD). Long runs: `nohup … > log 2>&1 &`, poll the log, never pipe. Ledger every experiment: `docs/superpowers/extraction-iteration-ledger.md`.
- Edge fn (deployed test project uonuiadueykynbetxxrw) — no edge changes expected before Phase 4; back up before any redeploy.
