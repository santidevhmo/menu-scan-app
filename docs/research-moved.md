# Research lives in the knowledge base, not here

All Menu Scan research — competitors, market scans, onboarding captures, personas, prior art —
lives in the **`menu-scan-kb`** repository, not in this one.

> "This is a separate repository from the application. It is not part of `menu-scan-app` and its
> contents must never be committed there." — `menu-scan-kb/README.md`

**Read it through the skill:** type `/menuscan-research` (or `/menuscan-product` for the hub).
The skills are symlinked from `menu-scan-kb/skills/`, so they are always current.

On 2026-08-30 the phase-1 research tree was written here by mistake and relocated to
`menu-scan-kb/docs/research/`. Do not recreate `docs/research/` in this repo.

| Still lives here | Why |
|---|---|
| `.agents/product-marketing.md` | The working positioning document. That exact path is a contract with the `coreyhaines31/marketingskills` skills, which read it by convention. |
| `docs/mobile-app-design-phase-order.md` | Design-process **status** (`[x]`/`[~]`/`[ ]`) for this project. The KB holds knowledge, not state. |
