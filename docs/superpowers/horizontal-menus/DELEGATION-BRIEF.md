# DELEGATION BRIEF — Horizontal Menus (critical-path #4)

> **Read this file in full before doing anything.** It is the entry point for zero-context LLMs continuing this work. It overrides your defaults, except the repo's `CLAUDE.md` and `AGENTS.md` (below), which also govern — read both and follow them STRICTLY.

---

## ⚡ PROGRESS LOG (newest first — every planner session APPENDS here before ending)

- **2026-07-21 (🎯 H1.3 GATE PASSED — H1 recall-safe tiling CLOSED; eval 093; NEXT = H2 rotation):** All 3 wide menus ×3 through the new landscape tile geometry (~$2.6 live). **Polloteria (dense, release-critical): ALL 5 DIMS PASS ×3** + ruling-6 clean (40 real full recall + 2 Paletas, zero inventions, no crush). **Bistro: ALL 5 DIMS PASS ×3.** **Guest House (spacious): recall PASS ×3 — full 48/48 incl. all 12 Enhancements + 3 Sides, zero inventions**; options/section_context red by design (Shape-A splits, Phase-2-deferred). Two scorer tolerances landed this eval (both TDD'd + planner-adversarially-verified, committed `ae0cc03`+`1607ddd`): section-title PUNCTUATION fold (`Pa'Compartir`~`Pa Compartir`) + section-title SPELLING fuzzy match (`Sandwiches & Hamburguesas`→`Hamburgers`, a new-geometry misread; word-level exact|1-indel|shared-prefix≥5&≥50% — rejects entradas~ensaladas, GH splits still flag spurious). H1's eval-091 regression FIXED (landscape 0.65/0.35 overlap recovers seam-straddling columns + H1.2a merged-row split + null-grams twin-fold); portrait byte-identical. Ledger eval 093. **NEXT ACTION: H2 — ROTATION** (the camera core flow: sideways captures → upright, content-based detection + direction-aware 90CW/90CCW/180 per rulings 22–23; NOT built). H2.1 = $0/cheap-probe brainstorm of the mechanism (extraction-call `needs_rotation` text-direction signal vs try-rotations-scored-by-cheap-OCR); H2.2 build (client, cross-platform) + rotated fixtures (rotated twins of the 3 wide menus, one oracle each); H2.3 gate (rotated twin == upright twin; 6 vertical NEVER rotate). Then H3 wiring → H4 combined 9-menu gate + device. PARKED post-release: 1f cost-opt, Phase-2 Shape-A conventions (GH + polloteria deferred items = the Phase-2 eval set), ruling-20 bake-off.
- **2026-07-21 (H1.2b GATE-PREP LANDED `07b955a`, planner-verified — ruling 28 Enhancements=food + steak twin-fold; NEXT = GH Shape-A item-band tolerance → H1.3 gate):** Both follow-ups from eval 092 landed & independently verified. (1) **Ruling 28** — GH Enhancements `other`→`food` (draft + fixture); GH corrected-draft self-check now **ALL 5 DIMS PASS**. (2) **`nullGramsTwinFoldCandidate`** (merge.ts, tile-path/sectionLenient-scoped): folds same-section same-category exact/word-subset-name twins when both grams null + prices differ/one-null, keeping the larger non-null price — kills the Butcher's-Best overlap twins ($1 Mishima / $14 Ribeye). Planner independent re-verify: merge 26/26, GH self-check ALL PASS, from-scratch predicate replay on nikkori's 4 archived tile sources = **0 new folds** (portrait 5 byte-identical by scoping). Pushed. **NEXT ACTION: H1.3 gate-prep — GH Phase-1 item-band tolerance.** After folding the 3 steaks GH is ~52 vs 48±3; the 4 legitimate Shape-A splits (Seafood Plateau, Filet 7oz/10oz — ruling 1, Phase-2-owned) still exceed the band → GH fixture needs a Phase-1 item-band tolerance (polloteria-Ensaladas precedent; a Santiago fixture ruling). Then **H1.3 GATE ×3** — all 3 wide menus (polloteria dense, bistro, guest-house) through tiles, 5 dims + ruling-6 audit clean, zero real-dish loss (~$0.72–0.87/attempt, fresh approval). Then H2 rotation → H3 wiring → H4.
- **2026-07-21 (EVAL 092 — H1.2b: landscape overlap (A) vs whole-read union (B) both ran; A WINS — full GH recall + no dense crush; B injects dense phantoms; geometry committed `f9b2029`; NEXT = 2 gate-prep decisions):** Ran both approved approaches ($1.16, 4 runs). **A = landscape `gridCropRects` 0.65/0.35** (committed): GH **48/48 recall — all 12 Enhancements + all 3 Sides, ZERO inventions** (+7 = 4 Shape-A splits + 3 Butcher steak overlap-twins, both Phase-2 classes); polloteria **42 = exact eval-085 profile, NO crush regression**. **B = whole-read union** RECOVERS but **injects unprinted phantoms into dense polloteria** (`Chickies Waffle Sandwich`, `Dechado de Queso`, `Gyro (500gr)`) → REJECTED. Ledger eval 092. **NEXT ACTION — 2 gate-prep items for H1.3 (planner correcting an earlier oversimplification):** **(1) Enhancements category = Santiago ORACLE ruling, NOT a code fix.** The v1.2 filter (ruling 12) drops `category:"other"`; the corrected GH TRUTH labels the Enhancements `"other"`, so they survived only because GPT labeled them `"food"` this run. No structural discriminator separates them from the polloteria condiments ruling 12 kills (BOTH priced-under-a-section; a price rule regresses polloteria). Planner recommends ORACLE-CHANGE GH Enhancements → `food` (semantically a real premium add-on food section — Foie Gras/King Crab/Lobster; production already labels them food; polloteria untouched; residual = watch the ×3 gate for a flip-to-other). **(2) Butcher steak twins = merge fix** (grams-null twin-fold branch: same section+category, exact/word-subset name, differing/one-null price → keep larger non-null price; TDD + replay gate = zero new folds on the 6 vertical + polloteria). Gate note: even post-fold the 4 Shape-A splits keep GH over the 48±3 band → GH fixture needs a Shape-A item-band tolerance too (polloteria-Ensaladas precedent). Awaiting Santiago's ruling (1) before the executor block; (2) is planner-designed and ready.
- **2026-07-21 (H1.1 diagnostic DONE + H1.2a existence-tier fix LANDED, planner-verified & committed; Bug 1 tile-seam recall = H1.2b, NEXT):** H1.1 ($0 offline — cached OCR + tile-rect math) decomposed the two eval-091 losses. **(a) Enhancements loss = TILE GEOMETRY:** the 12-item compact 2-column block sits at normalized x[0.40,0.67] y[0.75,0.87], straddling the x=.40/.60 vertical tile seam — in tile2 the right column is clipped, in tile3 the left column sits exactly on the .40 boundary, so EVERY containing tile sees it cut off and `TILE_PROMPT_SUFFIX` ("skip cut-off items") correctly suppresses all 12. Fixable via more horizontal overlap (block width .27 > overlap .20) OR whole+tiled union (whole-read got all 12); do NOT touch the suffix. **(b) 3-Sides false-deletion = scoring formula, NOT OCR:** OCR read MISO MUSHROOMS / GRILLED BROCCOLINI / ROASTED ASPARAGUS VERBATIM, but each shares a merged 2-column row with a neighbor dish (`"MISO MUSHROOMS 15 PARMESAN TRUFFLE FRIES 16"`), and `bestLineSim=matched/max(cand,block)` scored 2/5=0.40<0.75 → dropped. **H1.2a fix landed & committed** (`splitPricedRow` inside `bestLineSim`: blocks with ≥2 bare-integer prices split into per-dish segments; <2 untouched so the description-mint guard survives). Planner-verified: the executor's own replay sweep was VACUOUS (ran on post-colocation cleaned dumps eval090/091/089b — inventions already gone); planner re-ran on the CANONICAL pre-colocation dumps → eval-068 46→40, 077 43→41, 078r1 40→39, 078r2 44→42, every known invention still dropped, zero resurrected; keep-side confirmed (the 3 Sides re-inserted into the eval-091 dump are KEPT by the new code). Full `deno test` blocked by a PRE-EXISTING unrelated `@/types/scan` alias error (diff touches zero `src/` files). Ledger: "H1.1 diagnostic + H1.2a". **NEXT ACTION: H1.2b — the tile-seam RECALL fix (Bug 1).** Root cause known; needs (1) Santiago's ruling between **landscape-only higher-overlap `gridCropRects`** (~$0.24/scan, portrait/nikkori byte-identical, must re-verify polloteria dense) vs **whole-read + tiled union for wide menus** (~2× cost, robust, must confirm colocation kills re-introduced whole-read phantoms), and (2) a ~$0.29 guest-house confirmation probe. Then H1.3 combined gate (all 3 wide menus ×3 through tiles) → H2 rotation → H3 wiring → H4.
- **2026-07-20 (RULING 27 — horizontal is LAUNCH SCOPE incl. rotation; roadmap RESTRUCTURED into H1–H4; NEXT = H1.1 $0 diagnostic):** Santiago corrected two planner misconceptions from the previous session: (a) hiding gallery import does NOT avoid horizontal menus — users photograph wide menus with the CAMERA by turning the phone, so captures arrive PORTRAIT-shaped with the menu rotated 90° (sideways); therefore rotation (old "Phase 3", now **H2**) is REQUIRED and launch-critical, not deferrable; (b) **1f = the dense-vs-not detector, PURELY a cost optimization** (parked post-release per ruling 24) — it is NOT the blocker. VERIFIED production reality: horizontal is currently UNSUPPORTED end-to-end — no rotation built, and the tile-all trigger is INERT because the client sends no dims (`analyzeMenu.ts` sends no `photo_dims`; `index.ts:362` calls `runPagedExtraction(photos, OPENAI_API_KEY)` with no dims → `photoDims=[]` → landscape short-circuit never fires). So the **eval-091 guest-house regression is HARNESS-ONLY, not live for users.** Also re-audited independently this session: **eval 085 polloteria ×3 had ZERO hallucinations** (42 = 40 real FULL RECALL + 2 Paletas; only minor name/grams misreads) — the tile stack does not invent; its only failure mode is recall loss on spacious menus (eval 091). Ruling 27 recorded (ROADMAP); roadmap RESTRUCTURED into the LAUNCH PLAN: **H1** recall-safe tiling (fix eval-091: Enhancements-column loss + existence-tier real-dish deletion) → **H2** rotation (content-based, direction-aware; rulings 22–23) → **H3** production wiring + e2e device → **H4** combined 9-menu gate + device. PARKED post-release: 1f cost-opt, Phase-2 size-variants, ruling-20 bake-off. **NEXT ACTION: H1.1 — $0 OFFLINE diagnostic** of the two eval-091 losses: (a) why tiling dropped the whole Enhancements column — tile-overlap/seam geometry vs model-skip (use tile-rect math + the cached `guest-house.mistral-ocr-2048q95.json`); (b) why the existence tier scored Miso Mushrooms/Grilled Broccolini/Roasted Asparagus <0.75 — OCR-evidence quality vs threshold. Output decides H1.2's fix approach (tile-geometry fix vs whole+tiled union) + the existence-tier real-dish guard. No live spend; then H1.2 → H1.3 gate (all 3 wide menus clean through tiles ×3) → H2 → H3 → H4. Nothing in flight; docs committed/pushed.
- **2026-07-19 (EVAL 091 — B3 guest-house through tiles FAILS HARD: tile-all LOSES the 12-item Enhancements section + existence tier FALSE-DELETES 3 real Sides; 34/48 vs 49 whole-read — disproves ruling-24 "tiling never hurts"; ESCALATING):** B3 ran (PID 88585, ~$0.29): FAIL all 5 dims, items 34/48. Planner ruling-6 audit vs draft: (1) the ENTIRE Enhancements section (12 add-on/sauce items) was extracted by NO tile — the whole-read got all 49; tiling fragmented the compact small-print column and the tile-edge skip rule dropped it; (2) the existence tier false-deleted 3 REAL Sides (Miso Mushrooms/Grilled Broccolini/Roasted Asparagus, bestSim 0.40–0.50) because the OCR read those lines poorly — dropping real dishes = the release-blocking class, first time the tier tripped on a real dish. HEADLINE: tile-all HURTS spacious wide menus (a CLASS risk, not a one-fixture quirk); release-critical menus (polloteria dense ✓, 6 vertical ✓) unaffected; guest-house is record-only/bake-off; immediate launch exposure limited (camera=portrait until Phase 3; gallery-import maybe hidden) but Phase 3 reintroduces wide images onto this path. Ledger eval 091. **NEXT ACTION — Santiago strategic call (planner recommends the $0 diagnostic FIRST):** (0, RECOMMENDED) $0 investigation — is the Enhancements loss overlap/geometry-fixable (offline tile-coverage check), and can the existence tier be made safe against real-dish deletion (why bestSim<0.75 on real Sides); report, THEN decide accept-vs-fix with data; (1) accept + park spacious-wide tile-recall post-release; (2) whole+tiled UNION for wide menus (~2× cost, colocation-phantom risk); (3) paid tile-geometry probe (more overlap/column-aware); (4) revisit discrimination via the whole-read RESULT. NOTE: the existence-tier real-dish deletion is a correctness concern beyond guest-house and worth the $0 look regardless. Production wiring + Phase-1 exit check are BLOCKED behind this decision.
- **2026-07-19 (RULING-26 FIX LANDED + verified; B2 = MET; NEXT = B3 guest-house-through-tiles ~$0.29 pending approval):** Executor landed `dba4c7b` (scorer substring tolerance; only `scripts/eval-extraction.ts`, +15/-1). Planner verified independently: matcher = `token.length>0 && normalize(option.name).includes(token)` (exactly as specced, non-empty guard present); ALL 5 score-dumps ALL DIMS PASS — eval-090 r1+r2 (r2's verbatim "Agrega queso a tus papas" upsell now tolerated) AND eval-085 r1/r2/r3 (short-"Queso" regression clean). **B2 = MET** — eval 090 = 2 clean reconfirmation runs through the ruling-24 deterministic tile path + eval 085's real polloteria ×3; ruling-24 routing introduced NO regression; no 3rd run (extraction stack byte-identical since eval 085). **NEXT ACTION:** Santiago's cost approval for **B3 = guest-house-THROUGH-TILES** (~$0.29, 1 run; `probe-tiles.ts` with PHOTO=GuestHouseMenu.png / guest-house fixture / DUMP_TAG=eval091 / PROBE_RUNS=1; guest-house is detector record-only per ruling 21 but tile-all now routes it to tiles, so confirm its extraction reads acceptably through the tile path — scored vs its fixture + ruling-6 photo audit vs GuestHouseMenu.png.draft.json; oracle-fit failures possible à la bistro since the fixture was built from whole-read output). Then production wiring block (index.ts `isValidPhotoDims` + client `photo_dims`, spec §1–2) → Phase-1 exit check → ruling-20 bake-off probe → Phase 2.
- **2026-07-19 (RULING 26 — printed upsells OK as option OR description; substring-tolerance scorer fix handed; B2 = MET on green):** Santiago's acceptance criterion on the eval-090 Queso miss: the upsell is fine emitted as an option OR left as description text — either passes. Recorded ruling 26 (ROADMAP; generalizes ruling 16): description form already passes (ungated); option form fixed by making `tolerated_option_names` match by SUBSTRING/contains. Executor fix block ($0, scorer-only TDD) handed to Santiago: `eval-extraction.ts:307` → `normalize(option.name).includes(normalize(name))` with a non-empty guard; tests (Queso short + verbatim tolerated; a non-queso untolerated option still FP); gate = re-score eval-090 r1+r2 → both ALL PASS AND eval-085 r1–r3 still ALL PASS (regression). **On green: B2 = MET** (2 clean reconfirmation runs + eval 085's real ×3; extraction stack unchanged since eval 085; no 3rd live run — low value). **NEXT ACTION:** collect the fix checkpoint (planner verify: substring matcher + tests + all re-scores green) → then **B3 = guest-house-THROUGH-TILES** (~$0.29 ×1, fresh cost approval; record + ruling-6 audit) → production wiring block (index.ts `isValidPhotoDims` + client `photo_dims`, spec §1–2) → Phase-1 exit check → ruling-20 bake-off probe → Phase 2.
- **2026-07-19 (EVAL 090 — B2 polloteria reconfirm: r1 clean, r2 options FAIL = ruling-16 Queso upsell emitted VERBATIM ["Agrega queso a tus papas"]; NO regression from eval 085; scorer-tolerance fix + B2-MET proposed):** Executor ran B2 (PID 94302, ~$0.49 of $0.87, stop-on-first-scored-failure): r1 ALL 5 PASS, r2 items/section_context/categories/grams PASS + options FAIL (5 FP). Planner ruling-6 audit of BOTH dumps: item-level CLEAN — 42 items each = 40 real (full recall) + 2 Paletas, zero inventions, no Petrica-class; r2 colocation dropped the two 300gr corrupted twins + kept/flagged the field-flakes exactly as eval 085 (tile stack byte-identical; ruling-24 routing not exercised by the harness). The r2 options FAIL is 100% ORACLE-FIT: the ruling-16 printed Queso upsell emitted this run as the verbatim `"Agrega queso a tus papas"@10` on 5 S&H cards instead of the short `"Queso"@10` (evals 082/085); the scorer matches `tolerated_option_names` by EXACT equality (`eval-extraction.ts:307`) so it missed the longer form. NOT a defect — model-phrasing variance on a blessed class. Ledger eval 090. **NEXT ACTION — Santiago decides (fixture/scorer, his call):** (1) RECOMMENDED — make `tolerated_option_names` matching SUBSTRING/contains (an option whose name CONTAINS a tolerated token is tolerated; safe — "queso" is an option-name substring only in this upsell class); $0 TDD; gate = re-score eval-090 r1+r2 → both ALL PASS; then B2 = MET (2 clean reconfirm runs + eval 085's real ×3; extraction stack unchanged) → B3 guest-house (~$0.29, fresh approval) → production wiring (index.ts isValidPhotoDims + client photo_dims, spec §1–2) → Phase-1 exit check → ruling-20 bake-off. Alternative to (1): add the verbatim string (brittle). Optional: a literal 3rd polloteria run (~$0.24) if strict ×3 is wanted — planner judges it low-value.
- **2026-07-19 (RULING 25: Petrica class PARKED — B1' bistro-through-tiles CLOSED; NEXT = B2 polloteria ×3, block handed):** Santiago ruled park-it-move-on on the eval-089b escalation (release speed; 1 leak in 6 audited tile runs). Ruling 25 recorded (ROADMAP): the unanchored same-price ≤len/3-edit corrupted-twin class is TOLERATED for release — ruling-6 audits RECORD it without blocking; every other unprinted/invented class remains release-blocking (narrows the 2026-07-14 ruling for exactly this shape); the sibling-aware twin rule + $0 sweep is parked POST-RELEASE (master roadmap list). Planner also verified for Santiago that nothing regressed from eval 085: git diff vs `037b49c` shows the ONLY production change is the ruling-24 routing; merge/postprocess/colocation byte-identical — Petrica is a pre-existing boundary case first exposed by bistro riding tiles. B1' CLOSED under ruling 25 (tile path beats bistro's whole-read baseline: 24/24 recall + all prices exact in both clean runs). **NEXT ACTION:** executor runs the handed B2 block — polloteria reconfirmation through the current HEAD tile path, DUMP_TAG=eval090, PROBE_RUNS=3, ~$0.24/run ≈ $0.73 est (cap $0.87, approved 2026-07-18 "B1' yes, keep B2 too"), stop-on-first-scored-failure; planner ruling-6-audits all dumps (Petrica-class recorded not blocking per ruling 25). Then B3 guest-house (~$0.29, fresh approval) → production wiring block (index.ts isValidPhotoDims + client photo_dims, spec §1–2) → Phase-1 exit check → ruling-20 bake-off.
- **2026-07-19 (EVAL 089b — B1' resume: scorer ALL PASS ×2 but r1 AUDIT-REFUSED — `Petrica` same-price corrupted twin survived beside OSTRICA; ×3 NOT MET; sibling-aware existence twin rule proposed, Santiago ruling pending):** Rulings A+B landed (`747b598`; all $0 gates green: eval-089-r1 re-score ALL PASS, self-checks, replays byte-identical) and the ×2 resume ran (~$0.49). r2 CLEAN (24 exact, prices perfect). r1: 25 items — real OSTRICA + `Petrica $252 (Salmón ahumado…)`, printed nowhere: the release-blocking duplicate class; scorer blind by design (rides +3 band, name differs). Mechanism (ledger eval 089b): 2-substitution 7-char twin with the SAME printed price → no anchor (1-edit matcher) → polarity can't touch it → existence tier's min(3,len/3)=2 allowance scores it 1.00 → kept via the deliberate keep-bias (which must protect Cabeza↔Cubeta, same 2-edit shape — global tightening REJECTED). **NEXT ACTION — Santiago decides:** approve the **sibling-aware existence twin rule** (drop an UNANCHORED bestSim≥0.75 item whose best printed line is owned strictly better by another final item at the SAME price; menu-generic, narrow) + its $0 validation sweep (extend replay-colocation to report would-drops; gates: Petrica drops on eval089b-r1, ZERO would-drops on eval-085 r1–r3 / eval-089-r1 / eval089b-r2 / nikkori ×3 / 077-078 dumps) → executor TDD into colocation.ts → fresh B1' ×3 (~$0.73, new approval) → B2 polloteria ×3 (~$0.87, doubles as the rule's regression exposure) → production wiring → Phase-1 exit.
- **2026-07-18 (EVAL 089 r1 — bistro THROUGH TILES: ruling-6 audit CLEAN, 24/24 recall, ALL prices exact (beats the whole-read baseline); scored FAIL section_context+categories = 100% oracle-fit; 2 Santiago decisions pending):** B1' ran once (~$0.24; harness param `b5569fa`; stopped on first scored failure per design). The tile path handled the spacious wide menu excellently: finals = exactly the 24 gated dishes, zero unprinted/dupes, every price exact; stack killed 4 tile artifacts on evidence (v3: Con Champiñones truncation; existence tier: Tóxica/MEDITERRÁNEA/IBÉRICA at bestSim 0.00; merge: SALMÓN twin) with zero false deletions — strong validation of ruling 24 on the "wasted-cost" menu class. Failures decomposed (ledger eval 089): (a) sections — finals say `PIZZAS BISTRO 28CM`; print stacks `28 CM` under the `PIZZAS BISTRO` header (tile concatenated the printed subtitle); fixture requires `Pizzas Bistro`; (b) categories — fixture's `["food","other"]` + `Pollo→other` pin encode the whole-read shape of the Agrega block that ruling 5 IGNOREs and v1.2 drops by design (unreachable under ruling 24). **NEXT ACTION — Santiago decides:** (A) ORACLE-CHANGE bistro fixture (categories:["food"] + tolerated_categories:["other"] + delete Pollo pin); (B) sections: RECOMMENDED scorer-side generic word-prefix tolerance (required section matched by an emitted title extending it with trailing tokens; ruling-14 precedent) vs brittle fixture rename. On approval: executor block ($0, TDD; gates = eval-089-r1 dump re-score ALL 5 PASS + every replay gate & fixture self-check byte-identical) → resume B1' (recommend re-scored r1 counts + 2 fresh runs, ~$0.49 of the $0.73 cap remains) → then B2 polloteria reconfirmation ×3 (~$0.87, already approved in principle) → production wiring → Phase-1 exit.
- **2026-07-18 (1f IMPLEMENTATION LANDED + PLANNER-CLEARED `24940a7`; routing proven at $0; NEXT = Santiago's B1' approval — bistro-through-tiles ~$0.73 cap):** Executor delivered the ruling-24 block TDD-clean: landscape short-circuit in runPagedExtraction (single-landscape → needs_crops:[0] with zero extract calls; mixed scans extract portrait pages only, needs_crops = landscape ∪ portrait-dense/failure ascending; portrait/dims-absent call shapes byte-identical), suffix + landscape params fully deleted. Planner verified independently: diff surgical (2 files), full suite 130/0 fresh, repo-wide suffix sweep 0 hits, probe-detector re-run WITHOUT --allow-net → PolloteriaMenu/BistroMenu/GuestHouseMenu each needs_crops=[0] deterministically (zero API calls possible — the eval-060 blind spot is closed BY CONSTRUCTION). **Phase-1 exit reading proposed to Santiago:** eval 085 already gated polloteria ×3 through the tile path with the harness force, and the force is now literally the production behavior (deterministic + unit-pinned) — so B2 (polloteria e2e re-run, ~$0.87) is arguably redundant; recommend SKIP (Phase-4 combined gate re-covers e2e) and spend only on the genuine unknowns. **NEXT ACTION:** Santiago's cost approval for **B1' = bistro-THROUGH-TILES quality probe** (~$0.24/run ×up-to-3 stop-on-first-failure ≈ $0.73 cap; gate = bistro's 5 scored dims vs its fixture + ruling-6 photo audit vs BistroMenu.png.draft.json; scored failures get planner diagnosis before any tile-path verdict — oracle-fit issues à la eval 082 are possible since the fixture was built from whole-read output). Prerequisite inside the same block: $0 probe-tiles.ts menu parameterization (PHOTO/fixture/DUMP_TAG are polloteria-hardcoded consts → env-var override, polloteria defaults keep replays byte-compatible). Then B3 guest-house (~$0.29 ×1, record+audit), production wiring block (index.ts isValidPhotoDims + client photo_dims, spec §1–2), Phase-1 exit check, ruling-20 bake-off probe.
- **2026-07-18 (RULING 24: TILE-ALL-HORIZONTAL ADOPTED — 1f detection abandoned as release scope; discrimination parked POST-RELEASE; NEXT = collect executor checkpoint for the $0 implementation, then B-ladder):** Santiago ruled on the eval-088 escalation: discrimination is a cost nice-to-have, not release-critical — prioritize release speed. Ruling 24 recorded (ROADMAP; supersedes ruling 21's suffix mechanism + the same-day keep-discriminating cost ruling): landscape pages (`photo_dims` w>h) → `needs_crops` deterministically, NO model call; `LANDSCAPE_PROMPT_SUFFIX` deleted; `photo_dims` plumbing retained; portrait path byte-identical (model self-report stays). Docs synced: 1f spec Amendment v2 (deterministic mechanism; suffix/verdict-table/fallback sections SUPERSEDED; revised B-ladder), container ROADMAP (status, ruling 24, 1f step, deferred item), master roadmap POST-RELEASE list + AGENTS.md status line (main repo, docs-only), memory. Executor task block for the $0 implementation authored and handed to Santiago: extract.ts landscape short-circuit in `runPagedExtraction` + suffix/`landscape`-param deletion + test rewrite (landscape stub-not-called, mixed-scan union, portrait pin) + $0 probe-detector verification (3 wide menus → `needs_crops=[0]` each, ZERO API calls). **NEXT ACTION:** collect the executor checkpoint (diff + fresh test totals + $0 probe output), planner-verify (portrait pin green, suffix gone repo-wide, only authorized files touched), then B-ladder with FRESH cost approvals per launch: B1' bistro-THROUGH-TILES quality (~$0.24/run, up to 3 — bistro never rode the tile path; THE risk of tile-all), B2 polloteria e2e trigger-live ×3 (~$0.87, expect eval-085 reproduced), B3 guest-house (~$0.29 ×1), each with ruling-6 photo audits; then the production wiring block (index.ts `isValidPhotoDims` + client `photo_dims` per the spec's still-valid §1–2) → Phase-1 exit check → ruling-20 bake-off probe → Phase 2.
- **2026-07-18 (EVAL 088 ~$0.03: text-size hypothesis REFUTED — Bistro (10.9px) sits BETWEEN Polloteria (9.2) and Nikkori (12.2); cheap single-signal dense detection CLOSED as a class; recommendation = deterministic tile-all-horizontal, Santiago ruling PENDING):** Fresh-brainstorm session (Santiago declined the eval-087 option list, then redirected to hypothesis-driven grounded testing + worldwide generalization). Planner hypothesis: dense ⇔ printed per-line text height below a legibility floor after GPT's 768-shortest-side rescale, measured from Mistral OCR block geometry (the un-crushed 2048px path). Predeclared gate: Bistro ≥1.5× above Polloteria's 9.2px, else auto-fall-back to tile-all. Executor built+ran probe `fbc5f1c` (7 OCR captures ~$0.028, durable caches `*.mistral-ocr-2048q95.json` in ~/Downloads/MenusTesting/ for ALL 9 menus now; polloteria/nikkori cache-hit anchors matched planner pre-computation exactly). RESULT: medians px-after-rescale — dense: polloteria 9.2, nikkori 12.2; not-dense: **bistro 10.9 (BETWEEN the dense pair)**, guest-house 12.1, verticals 13.9–18.0; p25 also interleaves, p75 INVERTED → no floor at any statistic → GATE FAIL, hypothesis refuted. With model self-report (086/087), items/MP (087), and physical text size (088) all dead on the same menu pair, the detection PROBLEM (not any detector) is the dead end: "dense" is a joint property of text size + content volume + packing. Blocks/MP (1.6× boundary gap, N=2 dense) noted but recommended AGAINST (third post-hoc fit to the same 9 points; ruling-7 fragility). Ledger eval 088. **NEXT ACTION — Santiago ruling:** adopt **deterministic tile-all-horizontal** (RECOMMENDED; evidence-backed reversal of the earlier keep-discriminating cost ruling: any landscape page → `needs_crops`, NO model call; DELETE `LANDSCAPE_PROMPT_SUFFIX`; keep photo_dims plumbing; dense wide scans get ~$0.05 CHEAPER — the discarded whole read is skipped; simple wide menus pay ~$0.24 vs $0.05, rare + partially offset). On approval: 1f spec revision (suffix sections superseded by the deterministic mechanism) → executor block ($0) → NEW B-ladder, fresh approvals each: **B1' = bistro-THROUGH-TILES quality probe (~$0.24/run — bistro has never ridden the tile path; the one real risk of tile-all)**, B2 polloteria e2e detector-live (~$0.87), B3 guest-house (~$0.29) → production wiring (index.ts photo_dims validation + client analyzeMenu photo_dims) → Phase-1 exit check → ruling-20 bake-off probe.
- **2026-07-18 (eval 087 — reword OVER-corrects: all 3 wide menus dense=false, Polloteria LOST; prompt-based detection CANNOT separate dense from non-dense; ESCALATING to Santiago with 3 options):** Reworded suffix (`14b0304`, planner-verified EXACT) directional ×1 re-probe (PID 87335, ~$0.15; a prior launch mangled the log filename → ~$0–0.05 ambiguous, negligible): Polloteria dense=FALSE items=44 ❌, Bistro dense=FALSE items=26 ✓, GuestHouse dense=FALSE items=49. The two wordings BRACKET the model (original = all dense; reword = all not-dense) without ever separating Polloteria from Bistro — GPT-4o perceives them as similarly dense on the rescaled wide image, so the suffix is a global dense dial, not a discriminator. Ruling-21's one reword round is spent → prompt-based detection is a dead end. Ledger eval 087. **NEXT ACTION — Santiago decides among:** (1) RECOMMENDED — **deterministic tile-all-horizontal** (in `runPagedExtraction`, any landscape page → `needs_crops` with NO model call; DELETE `LANDSCAPE_PROMPT_SUFFIX`; reuses the photoDims plumbing already built; robust, zero false-positives, ~$0.20/scan extra only on the rare *simple* wide menu — and it's what the original suffix effectively did anyway; note this REVERSES the earlier cost ruling, justified because prompt discrimination is now proven infeasible); (2) deterministic items-per-image-area proxy (~9 items/MP separates these 3, but magic-threshold generalization risk + FAILS on the densest menus whose whole read undercounts → looks not-dense); (3) a third middle-strength reword (likely futile — menus appear inseparable by prompt). Once chosen: implement (executor, $0 for #1) → re-probe → Phase-1 exit check. Production wiring (index.ts photo_dims validation + client) still deferred until the detector approach is locked.
- **2026-07-18 (COST RULING: keep dense-discrimination via reword, NOT tile-all-horizontal; final reword wording set; executor reword block handed):** Santiago asked the cost question. Planner answered from measured data: not-dense scan ~$0.05 (1 whole-photo vision call); dense/tiled scan ~$0.24–0.29 (~8–10 calls + 1 Mistral OCR); gap ~$0.20/scan (~4–5×); Stage-2 enrichment ~$0.03 is constant either way (doesn't change the gap). Planner RECOMMENDED deleting discrimination (deterministically tile all horizontal — most horizontal menus are dense anyway, so discrimination only saves ~$0.20 on the rare *simple* wide menu while costing a fragile, over-triggering prompt; deterministic all-tile is also slightly cheaper since it skips the detector call). **Santiago RULED: KEEP discriminating — do the reword** (preserve dense=false for simple wide menus; do NOT re-raise tile-all-horizontal). Final reworded `LANDSCAPE_PROMPT_SUFFIX` (packing-not-width per the eval-086 diagnosis; arrows removed for clean source): "This is a wide (landscape) menu photo. Base image_layout.dense on how tightly the items are packed together, not on the photo's width. Set dense=true only when many items are crowded close together with little space between them; if the items are few or clearly spaced out, set dense=false even though the photo is wide." **NEXT ACTION:** executor Block A ($0) — swap the const + its verbatim-pin test + the rationale comment; planner verifies the wording landed EXACTLY (executor deviated on this const once — 73e99da); then Block B = directional ×1-each re-probe (`PROBE_RUNS=1`, ~$0.15, approved via the strategy choice) — pass = Bistro dense=false AND Polloteria dense=true (GuestHouse recorded); if it separates → ×3 gate (~$0.45, fresh approval); if it can't separate them → escalate with the fallback ladder (detail:\"high\" on landscape phase-1 → deterministic force-tile).
- **2026-07-18 (B1 detector probe RAN ~$0.45 [eval 086]: suffix fires on dense but OVER-TRIGGERS — Bistro FP; NEXT = ruling-21 reword round pending Santiago wording sign-off):** Executor launched `probe-detector.ts` ×3 on Polloteria/Bistro/GuestHouse (detached PID 62391, 9 phase-1 calls, all done; temp=0+seed → 3/3 identical per menu). Planner audit vs the gate: **Polloteria DENSE 3/3 ✅** (eval-060 blind spot BROKEN — detector now fires on the dense wide menu), **Bistro DENSE 3/3 ❌** (must stay dense=false per ruling 21 — cost), GuestHouse DENSE 3/3 (record-only). GATE FAIL on Bistro non-regression: the suffix over-triggers, flipping EVERY landscape photo to dense. Root-cause hypothesis: the approved wording primes size→dense ("scaled down… text can look small… if you cannot read every item → dense"), applied to any wide image, overriding the base prompt's packing criterion. Ledger eval 086. **NEXT ACTION:** ruling-21's ONE reword round (~$0.15 pre-authorized-in-principle) — reframe the suffix to base density on item PACKING/CROWDING, not width, with an explicit "spacious/few-item wide = dense=false" anchor; planner recommends `LANDSCAPE_PROMPT_SUFFIX` = "This is a wide (landscape) menu photo. Judge image_layout.dense by how tightly items are packed, not by the photo's width: many crowded, closely-spaced items → dense=true; few or well-spaced items → dense=false, even on a wide photo." Wording needs Santiago's sign-off (first sentence approved verbatim + pinned by a test). Plan: executor updates the const + verbatim-pin test → directional ×1-each re-probe (~$0.15) → if Bistro flips to not-dense AND Polloteria stays dense → ×3 gate (~$0.45); if it can't separate them → escalate with the fallback ladder (detail:"high" on landscape phase-1 → deterministic force-tile with a density proxy). $0.45 spent this leg; production wiring still deferred until the detector gate passes.
- **2026-07-18 (Block 1 + 1b LANDED + planner-verified; NEXT = B1 detector probe pending ~$0.45 approval):** Executor landed `92cc58e` (edge core: `LANDSCAPE_PROMPT_SUFFIX` + `landscape` flag threaded runExtraction/extractWithRetry/runPagedExtraction + `photoDims()` helper + probe-detector dims). Planner diff-audit CAUGHT a deviation: the committed suffix DROPPED the approved phrase "scaled down before you see it," — tests asserted the const, not its words, so it passed silently (exactly the planner-checkpoint value). Fix `73e99da` restored the verbatim wording + added a whitespace-normalized verbatim-pin test. Planner independently re-ran: **103 passed / 0 failed**; const now matches Santiago's approved sentence EXACTLY; only authorized files touched; byte-identical portrait pin green; production wiring (index.ts/client) still correctly deferred. Minor (not fixed, harmless): `deno fmt` reflowed a few pre-existing long lines in `photo-input.ts`. **NEXT ACTION:** Santiago's cost approval (~$0.45) for **Block 2 = B1 detector-only probe** — `PROBE_RUNS=3 deno run --allow-read --allow-write --allow-env --allow-net --allow-run scripts/probe-detector.ts PolloteriaMenu.png BistroMenu.png GuestHouseMenu.png` (9 phase-1 calls, detached nohup + poll, NO tiles/verify/OCR); gate: Polloteria dense 3/3, Bistro not-dense 3/3, Guest House recorded; confirm worktree `.env.local` (OPENAI_API_KEY) present first. On approval the planner writes the launch block with the approval stated in it.
- **2026-07-18 (1f SPEC WRITTEN + APPROVED; executor Block 1 [$0 edge core] authored + handed; NEXT = collect Block 1 checkpoint → B1 paid probe ~$0.45):** Planner ground-verified the exact wiring in real code before designing: client `analyzeMenu.ts:202` extract body (photo objects already carry `.width/.height`), edge `index.ts:356` extract-stage → `runPagedExtraction`, the `tile/page` suffix chain in `extract.ts` (`runExtraction` prompt assembly line 259-260; `extractWithRetry`; `runPagedExtraction`), `request-validation.ts` `isValidOcrPhotos` precedent, `probe-detector.ts` + `photo-input.ts` (`MENU_DIR`, `productionPhotoData`, `sh`). Design re-presented to Santiago in PLAIN language (he flagged the jargon; entire exchange kept jargon-free). Sentence APPROVED VERBATIM: `LANDSCAPE_PROMPT_SUFFIX` = "This is a wide (landscape) menu photo, scaled down before you see it, so packed text can look small. If you cannot clearly read every item across the full width, set image_layout.dense=true." Oracle detector verdicts CONFIRMED by Santiago: Polloteria dense=true 3/3 REQUIRED, Bistro dense=false 3/3 REQUIRED (+ its item output must not regress — the suffix rides the *used* call on a non-dense landscape page), Guest House record-only. Spec written + self-reviewed (2 catches folded in: B3 bistro item non-regression; fallback ladder reword→detail:high→deterministic-force): `docs/superpowers/specs/2026-07-18-detector-trigger-1f-design.md`. Executor **Block 1 ($0, NO API calls)** authored + handed to Santiago: `extract.ts` gets `LANDSCAPE_PROMPT_SUFFIX` + a `landscape` flag threaded through runExtraction/extractWithRetry/runPagedExtraction (new `photoDims` param → `isLandscape(i)` = w>h; suffix appended only when `landscape && !tile`) + `photo-input.ts` `photoDims()` helper (sips) + `probe-detector.ts` passes real dims; TDD tests A (prompt: suffix on landscape phase-1 only, byte-identical portrait via default call, tile never gets it) + B (routing: absent/portrait→false, landscape→true, via injected stub); deno fmt/check/test; commit+push; HARD STOP. **Deferred to a POST-B1 wiring block** (NOT needed for any probe — the harness calls `runPagedExtraction` directly, bypassing index.ts/client): `index.ts` `photo_dims` validation via a new `isValidPhotoDims` (mirror `isValidOcrPhotos`) + client `analyzeMenu.ts` sending `photo_dims: photos.map(p=>({width:p.width,height:p.height}))`. Deferred deliberately so we don't wire production for a suffix B1 hasn't proven; the plumbing is fallback-agnostic (both suffix and deterministic-force use the same photo_dims). **NEXT ACTION:** collect executor Block 1 checkpoint (diff + full test output), planner-verify (byte-identical pin green, existing suite unregressed), then relay **Block 2 = B1 detector probe** (`PROBE_RUNS=3 deno run --allow-read --allow-write --allow-env --allow-net --allow-run scripts/probe-detector.ts PolloteriaMenu.png BistroMenu.png GuestHouseMenu.png`, ~$0.45) ONLY after Santiago's fresh cost approval — gate: Polloteria dense 3/3, Bistro not-dense 3/3, Guest House recorded. Then B2 e2e polloteria (~$0.87), B3 bistro/guest-house non-regression (item output too), the production-wiring block, then Phase-1 exit check → ruling-20 bake-off probe → Phase 2. $0 spent; planner changed NO production code this session (spec + brief docs only).
- **2026-07-18 (1f DESIGN SESSION $0: Option A approved at approach level — client `photo_dims` + landscape-scoped P1 density suffix; rulings 21–23 recorded; Phase 3 clarified NOT BUILT and promoted to camera-core-flow critical path; NEXT = present full design sections → spec → executor block):** Planner (brainstorming skill) explored the wiring surface: client `src/lib/analyzeMenu.ts` extractMenu (passthrough/needs_crops flow; `buildExtractPagesBody` ocr_photos = the exact optional-field precedent to copy), edge `request-validation.ts` (isValidOcrPhotos pattern), `extract.ts` suffix mechanism (TILE/PAGE_PROMPT_SUFFIX via tile/page flags in runExtraction), `probe-detector.ts` (existing, reusable as the 1f probe vehicle). Options weighed with Santiago: **(A) client-declared per-photo dims** — stage:"extract" gains optional `photo_dims: {width,height}[]`; the client already trusts these values to cut tiles; iPhone/Android identical; edge appends a landscape-only (w>h) P1 density suffix; field absent → byte-identical today (fail-open, portrait pinned) — **APPROVED**; (B) edge binary header parsing REJECTED (EXIF-rotated camera JPEGs swap raw dims; ~100+ lines to recover info the client owns); (C) deterministic force-tile-on-landscape REJECTED as primary (bistro would pay ~5× every scan) but RETAINED as the documented fallback lever if probes show the suffix can't fire reliably; (D) Santiago-suggested capture-time user prompt ("is the menu horizontal?") REJECTED for 1f (answers orientation not density; taxes every core-flow scan) — parked as a Phase-3 fallback. **FP policy (ruling 21):** polloteria dense fires 3/3 REQUIRED; bistro stays dense=false 3/3 REQUIRED (ONE suffix-rewording round ≈$0.15 before escalating back); guest-house RECORD ONLY — either verdict acceptable, its non-regression probe gates whichever path production routes it to. **Phase-3 ground rules (ruling 22):** production has NO sideways-menu handling today (a sideways capture returns garbage — Santiago now knows this explicitly); detection must be CONTENT-BASED from the image (NEVER device sensors — user ruling; also gravity is blind with the phone flat over a table) and rotation DIRECTION-AWARE (90CW/90CCW/180 — never hardcoded 90°). **Capture-flow constraints (ruling 23):** the app's capture screen makes every camera photo PORTRAIT (users flip the phone for wide menus → sideways-text-inside-portrait photos), so the camera core flow needs 1f AND Phase 3; order forced 1f → Phase 3; 1f alone already serves gallery-imported wide photos; Santiago is CONSIDERING hiding gallery import pre-release (NOT decided); everything must run on iPhone AND Android. Probe ladder estimates (fresh approvals at launch; the prior-era ~$0.90 approval stays STALE): B1 detector-only ×3 × 3 menus ≈ $0.45; B2 polloteria end-to-end with detector live ×3 ≈ $0.87; B3 bistro/guest-house non-regression ≈ $0.15–0.60. AGENTS.md status line synced (main repo, docs-only commit). **NEXT ACTION:** brainstorming flow is MID-STREAM — approach approved but design sections NOT yet individually presented/approved. Next planner: present the full 1f design in sections (architecture, exact suffix sentence [single-variable discipline], `photo_dims` validation, portrait byte-identical pin, harness mirror via fixture dims, probe ladder + costs), write `docs/superpowers/specs/2026-07-18-detector-trigger-1f-design.md`, spec self-review, Santiago spec review, then writing-plans → executor TDD block. $0 spent, no code changed this session.
- **2026-07-18 (EVAL 085: POLLOTERIA ×3 GATE MET — 3/3 scorer + 3/3 ruling-6 audits; step 1e CLOSED; NEXT = 1f detector trigger):** After eval 082 (all failures oracle-fit → rulings 16–18: Queso tolerated_option_names, Ensaladas Shape-A unchecked pins, punctuation fold — Block 5 `e104d8f`), eval 083 exposed the stack's FIRST false deletion (real Cheesey Bacon killed via block-order contradiction anchor; plus Agua/Aguacate pin collision) → Block 6 `19c354a` = ownership-ranked contradiction anchors + multi-word keys (ruling 19: structural math only, menu-generic); eval 084 went 3/3 scorer but 2/3 audited (r2 kept a corrupted C&W twin via cross-VERIFICATION anchor theft — mirror bug) → Block 7 `05ab934` = verify scan uses the same ranked list (1-line fix). Eval 085 (retag `037b49c`, ~$0.72): **ALL PASS ×3 + audits CLEAN ×3** — 42 items every run (40 real FULL RECALL + 2 Paletas), zero unprinted, zero duplicates; both 300gr twins dropped with verified siblings in r1; r2/r3 needed zero interventions. Gate judged MET per Phase-1 criteria + ruling 6 — with the standing caveat that the detector was bypassed/forced (probe harness). Ledger evals 082–085; total 07-18 spend ≈ $2.75. **NEXT ACTION:** planner designs step 1f — the aspect-scoped detector trigger (edge reads image dims from the file header; landscape-only P1 density suffix; portrait NEVER sees it) + bistro/guest-house false-positive/non-regression probes; brainstorm → spec → executor TDD block → probe with cost estimate (detector probes ≈ $0.15–0.30/menu; a prior-era ~$0.90 approval is STALE — re-confirm at launch). After 1f + Phase-1 exit check: **ruling 20 — scoped extractor bake-off probe BEFORE Phase 2** (current stack vs Mistral-OCR→LLM-grouping hybrid, same fixtures/gates, cached-OCR $0 work first; the 2026-06-23 extractor question, now under the real architecture); then Phase 2 (Shape-A conventions + mandatory Ensaladas sections restore + promote tolerated pins), Phase 3 rotation, Phase 4 combined gate. Research report #2 archived: `2026-07-18-external-research-prior-art-report.md`.
- **2026-07-18 (Eval 082 r1 on position-verified tiles: CLEANEST EXTRACTION EVER — zero defects, ruling-6 CLEAN, failures 100% oracle-fit; rulings 16–18 approved; Block 5 + fresh ×3 next):** Block 4 landed (`7a417a9` — node/pngjs cutter, planner hash-verified byte-identical to the validated true crop; npm:pngjs abandoned — deno 2.8 check can't type-resolve untyped npm CJS; echo prefix fix 39→36 exact). Eval 082 r1 (retag `32102ef`, ~$0.24): items/categories/grams PASS (cats+grams first-ever live); TostiBoneless + Dedos $199 read correctly for the first time in campaign history; recall 39/40 (Parmesano skipped by tile 1; tile 2's hallucinated copy correctly killed by v3; advisory named it); Ensaladas native Shape A with printed prices; colocation inert-clean (0 drops/flags). FAILS = 7 printed `Queso $10` upsell options + 3 Shape-A G-options + `Pa Compartir` apostrophe variance — all oracle-fit. RULINGS 16–18 approved (ROADMAP): tolerated_option_names scorer field ["Queso"], Ensaladas Shape-A tolerated-now/required-at-P2 (unchecked pins), section-title punctuation fold (production, tile path). Standing cost approval for test runs granted. **NEXT ACTION:** executor Block 5 ($0, in chat): scorer field + fixture entries (ORACLE-CHANGE approved) + punctuation fold, gates = eval-082-r1 dump re-scored through fold+new scorer/fixture must go ALL 5 DIMS PASS + all prior replay gates unchanged; planner checkpoint; then fresh ×3 = eval 083 (~$0.73, approved) — best gate odds in campaign history.
- **2026-07-18 (Eval 081 r1: SECOND sips bug — cropOffset 0,0 = CENTER crop; run harness-invalid but stack scored ZERO inventions; Ensaladas read in native Shape A; Block 4 pending):** r1 (~$0.24 of $0.73) FAILED 4 dims, all traced to ONE cause: `sips --cropOffset 0 0` silently center-crops (dims correct → Block 1's dims-only assertion passed) — the photo's top-left corner (Entradas column + both headings + Grandes/Jumbo) was in NO tile; archive raw traces prove tile 1 NEVER extracted an Entradas card in any run ever (the full-photo bug-tile was their only supplier). Production client (expo crop rects) unaffected. Despite invalidity: zero unprinted finals (ruling-6 clean), 2 correct live sibling drops, recall advisory named exactly the missing cards, true tile 3 read Pa'Compartir/Ensaladas PERFECTLY — Ensaladas in native Shape A (`M (150gr)` + option `G (300gr)`), El Tendedero/Megacharola/Cubeta correct (historic misread class = full-photo-crush artifact). Plus 3 Paletas option-echo FPs (`Paletas Heladas <flavor>` prefix defeats dropOptionEchoItems' exact-name rule; price guard would hold). Ledger "Eval 081 r1". Oracle note: print reads `Salsa's` (draft typo `Salas's`; nothing pinned). **NEXT ACTION:** executor Block 4 ($0, in chat): tile-cut v2 = pure-JS crop via vendored pngjs 3.4.0 (NEVER trust sips crop again; synthetic-pixel POSITION tests — dims assertions proven insufficient) + prefix-tolerant dropOptionEchoItems (TDD + $0 eval-081-dump validation) → planner checkpoint → FRESH ×3 (~$0.73, ~$0.49 unspent rolls in; Santiago re-confirms at launch).
- **2026-07-18 (Blocks 1–3 LANDED + checkpointed; eval-080 research reviewed; live ×3 launch block handed):** Executor landed `91b8805` (verified cutter — planner fresh-run: all 4 tiles 1364×943), `6e9e457` (colocation v1.3 — all 7 replay gates EXACT: eval-068 46→40 same 6, nikkori 0×3 inert, new full-photo gates 43→41/40→39/44→42 dropping exactly the ruling-13 inventions; jest waived — repo has no JS test runner), `bc389b5` (ruling-14 scorer tolerance; executor TDD caught a planner spec bug — 4-char `papa` needed a narrow short-plural rule; guards Nico≠Pico + Buffalo≠Barbecue proven; value strictness intact). Ledger "Eval 081 (implementation)". Separately: prior-art research report #2 reviewed as eval 080 ($0) — NOBODY has eliminated cross-card recombination (DoorDash: guardrail classifier + human backstop, no numbers); our v3 mention-guard + never-crop-before-OCR + binding-error audits independently confirmed; we're already on Mistral OCR 4 (title-typed blocks = free printed-header signal, parked); 5 trigger-gated hypotheses added to ROADMAP ("Parked hypotheses" section: doc-anchoring contingency, title-blocks for Phase 2, v2 localized re-read, per-scan trust score, bake-off additions incl. copy-only constrained decoding). **NEXT ACTION:** Santiago pastes the live ×3 launch block (in chat) to the executor — retag probe-tiles eval078→eval081, commit+push, detached nohup launch, ~$0.24/run ×3 stop-on-first-scored-failure ≈ $0.73 hard cap (restated + confirmed at launch); executor reports RAW log + dumps and never declares the gate; planner ruling-6 photo-audits EVERY run (incl. ALL-PASS runs) against the draft + full-photo OCR before any green call. First-ever attempt on TRUE production geometry — watch: recall (Ensaladas region now truly imaged; 40/40 possible), new tile-3-region phantom classes (doc-anchoring is the parked contingency), OCR determinism assumed from eval-079 (×3 identical).
- **2026-07-18 (RULING 15 APPROVED — spec v1.3 + cutter fix; executor blocks 1–3 handed to Santiago; prior-art research prompt delivered):** Santiago approved ruling 15 (ROADMAP; spec amendment v1.3 section written): evidence source = ONE Mistral OCR call on a 2048/q0.95 JPEG copy of the original, carried as optional `ocr_photos: (string|null)[]` parallel to `pages` in extract-pages (client reuses `compressImage`; absent → fail-open skip; per-tile OCR path deleted — it also never worked on-device: production tiles are bare base64 without the `data:` prefix Mistral needs). Planner authored THREE sequential executor task blocks (hard stop + checkpoint after each): (1) harness cutter fix — shared `scripts/tile-cut.ts` with dims assertion + lossless rotate-180 fallback for the sips flush-bottom-x=0 no-op, swapped into probe-tiles/eval-027-live/extract-draft/probe-paired-ambiguity/probe-nested-eval072; (2) v1.3 protocol + stage (colocation.ts signature `ocrPhotos: string[]`, extract.ts pass-through, index.ts validation, client analyzeMenu ocr_photos, harness mirrors via `compressedPhotoData(…, 2048, 95)`; replay-colocation accepts both cache shapes; $0 replay gates: eval-068 46→40 same 6 drops + nikkori eval027 r1-r3 0 drops + NEW 077r1 43→41 / 078r1 40→39 / 078r2 44→42 exact-named drops); (3) ruling-14 scorer lookup tolerance (word-level 1-edit ≥5-char fallback in category/grams expectation lookups ONLY; Papa-Sazonada-class; guards Nico≠Pico, Buffalo≠Barbecue; NOTE: wrong VALUES still fail — a misread 350gr vs pinned 300 remains a legitimate grams failure, v2-repair/bake-off territory). Prior-art deep-research prompt archived: `2026-07-18-external-research-prompt-prior-art.md` (has anyone SOLVED cross-card recombination — platforms/vendors/document-AI/academic + the 3 most adoptable mechanisms). **NEXT ACTION:** collect executor checkpoint 1 (cutter fix commits/tests), planner-verify (fresh dims run: all 4 polloteria tiles 1364×943), then relay Block 2; after Block 3 + all replay gates green → live ×3 ≈ $0.73 (pre-approved 2026-07-18, restate at launch; stop-on-first-failure; ruling-6 photo audit each run; watch recall — Ensaladas region properly imaged for the first time, 40/40 possible).
- **2026-07-18 (Eval 079 $0.09 planner diagnostic: eval-078 hypothesis CONFIRMED ×3 dumps; HARNESS CUTTER BUG found — "tile 3" has been the FULL PHOTO since eval 061; per-tile OCR REJECTED on measurement; full-photo 2048/q95 OCR is the proven evidence source; ruling 15 pending):** Santiago pre-approved the route (any spend $0–$0.72, ×3 grounding preferred). Planner replayed the archived 077r1/078r1/078r2 dumps through the PRODUCTION matcher (script `scripts/probe-tile-ocr-eval079.ts`; OCR deterministic ×3 on every input). Findings, all in ledger eval 079: (1) tile-edge OCR fragments (`"Nuggets (200gr)\nNuggets de pollo"` CUT-R) spuriously anchor BOTH description-minted inventions — mechanism confirmed, refined (merged priced cut-blocks also mint contradicted-alone keeps). (2) `sips --cropOffset` silently no-ops when x=0 AND flush-bottom → a 2×2 grid's tile 3 ALWAYS hits it → every harness "tile 3" since eval 061 was the full landscape-crushed photo (affects probe-tiles, eval-027-live cutTiles, extract-draft, probe-paired-ambiguity, probe-nested-eval072; production client UNAFFECTED — expo-image-manipulator). Explains tile-3 phantom sourcing, the 36-candidate overload, AND the missing-Ensalada recall gap (the tile-3-exclusive bottom-left region holds the Ensaladas cards and was never properly imaged; true tile 3 shows them crisply). Nikkori closed-gate DECISIONS stand (device runs used real tiles) but Phase 4 must run on a fixed cutter. (3) Corrected-union trials: per-tile OCR fails BOTH ways (tile 2 deterministically mega-blocks → its exclusive Sides region loses all evidence → existence tier false-drops 9–11 REAL cards; edge exclusion kills inventions but not the massacre; 40px-pad fixes tile 2 but needs edge codecs/client geometry — complexity tower). (4) WINNER, deterministic ×3: ONE OCR of a 2048/q95 JPEG of the original (the existing ticket-#3 client transform) → drops exactly the audited inventions across all 3 dumps (43→41, 40→39, 44→42), zero false drops, Papa Sazonada kept 1.00, readability 0.82–0.88, 4× cheaper ($0.004/scan). **NEXT ACTION:** Santiago's ruling 15 — approve spec amendment v1.3 (extract-pages gains optional per-dense-page `ocr_photo` = 2048/q95 copy; colocationStage OCRs it in ONE call; per-tile OCR path deleted; absent field → fail-open skip; matcher/polarity/v1.1/v1.2 unchanged) + the harness cutter fix. On approval: executor blocks (1) cutter fix $0 → (2) v1.3 protocol+stage TDD $0 (replay gates: eval-068 46→40 + nikkori 0×3 byte-identical, NEW 3-dump gates 41/39/42 exact) → (3) ruling-14 scorer edit-tolerance TDD $0 → fresh live ×3 ≈ $0.73 on TRUE production geometry (stop-on-first-failure, ruling-6 photo audit; recall gap may close — watch for 40/40).
- **2026-07-17 (Eval 078 ×3 attempt: r1 first-ever scorer ALL PASS but 0 audited greens — description-minted names survive via per-tile OCR; instrumentation next):** Fix `6f7545d` landed (loose gate 5→4; replays byte-identical). r1 scorer ALL PASS (milestone) but ruling-6 audit found `Ensalada de Pollo (350gr)` in finals — printed nowhere as a name-line (description-mint family); r2 scored-fail on the Papa Sazonada MISREAD-NAME-vs-scorer-expectation matching (card was KEPT — fix worked; 2-edit name exceeds the scorer's one-indel tolerance) and also kept flagged unprinted `Nuggets de Pollo $99`. Stack integrity held: zero false drops across both runs. HYPOTHESIS: per-tile OCR cuts prose at tile edges; fragments like `Nuggets de pollo` become short blocks that spuriously anchor description-minted names (same items score <0.75 vs the uncut full-photo OCR). BLOCKED ON INSTRUMENTATION: colocationStage persists neither its OCR blocks nor verdicts — audits are blind on live runs. Ledger eval 078. **NEXT ACTION (in order; rulings 13–14 landed same day):** (1) $0 harness change — persist per-tile OCR blocks + per-item verdicts into probe dumps; (2) one diagnostic live run (~$0.24, needs approval) to capture the tile blocks; (3) decide tile-edge-block exclusion vs full-page OCR on that evidence; (4) spec + TDD the scorer expectation-lookup edit tolerance per ruling 14 (scorer-side only); then a fresh ×3 attempt. Ruling 13 confirms both eval-078 survivors as inventions (photo-adjudicated), so the tile-cut hypothesis targets are certain. Budget note: $0.48 of the last $0.72 spent, run 3 unrun.
- **2026-07-17 (Eval 077 r1: FIRST live items/options/section_context pass; one short-token false drop; ×3 gate approval pending):** v1.2 landed (`c0a2bfd`) + replays byte-identical. Live r1 (~$0.24, budget now exhausted): salsa class GONE, items/options/section_context ALL PASS (first time ever), 2 sibling drops + 2 correct invention drops; FAIL categories+grams from ONE false drop — `Papa Sazonada (350gr)` (real Papas Sazonadas card, singular misread): 4-char `papa` can't fuzzy-match `papas` (≥5 gate) → unanchored, bestSim 0.50 → deleted. Planner-validated fix ($0 sweep): `looseTokenMatch` length gate 5→4 rescues it (1.00) and changes zero invention scores. Ledger eval 077. **NEXT ACTION:** Santiago's approval for (a) the one-char fix + regression test + $0 replays, (b) the formal ×3 exit-gate attempt (~$0.72 + OCR cents, stop-on-first-failure, photo audit per run). Then detector trigger (1f) per container ROADMAP.
- **2026-07-17 (Eval 076 r1: stack kills 11 fakes live; last failing class = cat='other' condiment panels; v1.2 rule pending approval):** v1.1 landed (`84b3f3c`; executor's hardcoded `anchored:false` bug was caught by the Paletas replay gate — one-line fix + regression test; planner re-verified replays: polloteria 46→40 exact, nikkori inert 0 drops ×3). Live r1 (`fdb6836`, ~$0.24): 4 sibling drops + 7 existence drops (all photo-plausible fakes with fresh names — Chickies Bacon, Boneless Elote, Super Cheesy Fries…) + 2 correct flags; FAIL only items 51/40 + section_context + categories, ALL from one cause: 9 printed sauce/aderezo/topping-panel items, every one GPT-labeled `category:"other"`. Archive survey: 42 'other' among 4,037 items ever, zero real dishes — the model's own label is a clean structural filter. Ledger eval 076. **NEXT ACTION:** user approval for spec v1.2 (tile path drops cat='other' beside the per-tile drink filter) → $0 replays → the one remaining authorized live run (~$0.24); a pass starts the formal ×3 exit-gate counter (fresh cost approval then).
- **2026-07-17 (Eval 075 $0 + ruling 11: existence tier v1.1 APPROVED; implementation + live ×2 next):** Planner ran the loose-name discriminator over all archived dumps with the user-corrected draft as truth. Polloteria: perfect separation (real misreads 1.00 — TortiBoneless/Tenderazo/Cabeza/Megachorla; all 6 inventions ≤0.67, including 2 newly exposed stable ones: Sour Cream Cheese Fries, Ensalada de Pollo a la Plancha). Nikkori counter-evidence: its real rolls score 0.00–0.07 (OCR can't render the names) → a global rule would delete the menu → mandatory per-menu readability gate (tier active only when ≥50% of non-drink items anchor; nikkori ≈12% → inert). Santiago approved the spec amendment (v1.1 section in the spec; ruling 11) and using the remaining ~$0.48 for live ×2 of the full stack. Simulated: eval-068 46→40 EXACT; nikkori 0 drops ×3. **NEXT ACTION:** executor task block — implement the tier in colocation.ts (TDD), rerun all replays (gates: polloteria 40 exact with the 6 named drops, nikkori 0 drops ×3), then live ×2 (probe-tiles retag eval076, 2-run loop, stop-on-first-failure).
- **2026-07-17 (Eval 074 r1: stage WORKS live, but 4 fresh novel-name inventions evade it — items 46/40 FAIL; $0.48 of approval unspent; options pending):** Retag `probe-tiles` → eval074, one launch, stopped on r1. The stage ran in production (2 correct evidence drops, 3 correct flags), all other dims except items+grams passed. $0 audit: the +6 overage = tolerated (2 Paletas + 2 Phase-2 reps) + 4 NOVEL-NAME inventions in Especialidades (`Boneless el Pollo`, `Tender el Pollo`, `Boneless el Pollo Vegetariano`, `Chicken Bacon` — OCR-grep: names printed NOWHERE). Root cause: unanchored items can't be contradicted; the unanchored bucket mixes inventions with real-but-misread names (El Tenderazo←El Tendedero etc.), so unanchored≠deletable. Candidate discriminator: loose word-level name-similarity to printed name-lines separates misreads (1-2 edits from a real line) from inventions (no close line) — UNTESTED. Ledger eval 074. **NEXT ACTION:** user chooses: (a) recommended — $0 replay experiment (eval 075) testing the loose-name tier against ALL archived dumps (068/074/nested/nikkori×3) with gate = all known inventions separable at zero real-card loss, then implement + spend the remaining live budget; (b) spend $0.48 on 2 more as-is runs (low value, same class will recur); (c) defer inventions to a user-facing confidence flag (product decision). Do NOT relaunch 074 as-is without a ruling.
- **2026-07-17 (Eval 073: co-location v1 IMPLEMENTED + replays PASS; awaiting live ×3 approval ~$0.72):** Executor landed `13012fe` (colocation.ts + wiring, 82+21 tests, planner diff-reviewed) and `ad4403f` (replay tool). Replays: polloteria eval-068 dump 46→42 with exactly the 4 phantom drops and ZERO real loss (the 5th predicted drop was already merge-folded into the real Boneless Jr — flagged, correctly kept; 42 = 39 real + 2 Paletas + 1 Phase-2 Alitas = zero unprinted, Phase-1 invention criterion met on the replay); nikkori eval027 r1/r2/r3 = 0 drops ×3 (false-deletion guard clean, one $0.004 OCR call now cached). Flags catalog the v2 repair class incl. Dedos $119→print $199. Full detail ledger eval 073. **NEXT ACTION:** Santiago's cost approval for the live ×3 (eval 074, step 1e): probe-tiles pattern, production path with real per-tile OCR, ~$0.24/run ×3 stop-on-first-failure ≈ $0.72; gate = scored dims + photo audit + zero unverified drops. After that: detector trigger (1f), then Phase-2 per the container ROADMAP.
- **2026-07-17 (Dedos adjudicated $199; production co-location spec WRITTEN — ruling 10):** Santiago confirmed the card prints `Dedos De Queso (200gr) $199` (GPT misread $119; OCR correct — first reader-disagreement point, favors OCR; field repair still v2) and green-lit the spec. Spec landed: `docs/superpowers/specs/2026-07-17-production-colocation-stage-design.md` — new `colocation.ts` in the edge fn: per-tile Mistral OCR (~$0.016/dense scan, no client/protocol change), eval-071 v3 matcher port, sibling-aware polarity v1 (drop ONLY contradicted-with-verified-same-anchor-sibling; flag contradicted-alone; keep unverifiable; zero mutation), advisory unmatched-priced-lines log (recall instrument), fail-open, tile path only with portrait byte-identical pin. Verification ladder: unit tests → $0 replays (eval-068 → exactly 5 drops/41 items; NIKKORI false-deletion replay MANDATORY, needs one ~$0.004 OCR call) → live polloteria ×3 (~$0.72, approval at launch). Out of scope: nested geometry (rejected), salsa-echo rule (nested-only artifact), repair, twin-fold. **NEXT ACTION:** planner issues the executor implementation task block per the spec; the block's only paid item is the nikkori OCR call (~$0.004) — relay approval in-block; live ×3 approval comes later at launch.
- **2026-07-17 (Eval 072 RAN + REJECTED standalone; $0 cross-audits define the production design; 2 user decisions pending):** Santiago approved ~$0.72; harness `1f90f3b` (planner diff-reviewed) launched once, r1 hard-failed (61/40 items, spurious SALSAS/SAZONADORES) and stopped per design (~$0.22 spent). Ledger "Eval 072" has the full decomposition: nested zoom FIXES name misreads and RECOVERS the missing Ensalada, but mints new grams-phantoms, lets cross-source twins (El Tenderete/$546 vs El Tendedero/$468) escape merge, and turns the now-legible salsa/seasoning panel into 11 printed pseudo-items no printed-evidence check can kill. Planner $0 cross-audits (python port of the eval-071 checker, cached OCR): nested 62→7 contradicted (all true phantoms); eval-068 BASELINE 46→8 contradicted = 5 known fakes + Tender/Gyro (real cards, one misread grams field) + Dedos de Queso ($119 GPT vs $199 OCR — reader disagreement). KEY DESIGN RESULT: production polarity must be sibling-aware — contradicted WITH a verified same-anchor sibling → drop; contradicted ALONE → keep + repair/flag the field; unverifiable → keep (+ verified-sibling twin-fold). Simulated on baseline: 41/40 band pass with zero real-dish loss. **NEXT ACTION — TWO USER DECISIONS:** (1) photo-adjudicate the printed Dedos De Queso price ($119 or $199); (2) approve the planner writing the production co-location stage spec (edge fn, tile path only, sibling-aware polarity, fail-open, ~+$0.004/scan) — recall (missing Ensalada) and the salsa-echo rule are named follow-ups inside that spec, NOT new nested-tile iterations (nested geometry is rejected; do not re-probe it).
- **2026-07-17 (Eval 071 CLOSED: 14/14 FULL PASS on the co-location replay; NEXT = Eval 072 cost approval):** Santiago approved ruling 9 (3-way semantics + alitas ORACLE-CHANGE, `cad2a24`). Executor landed v2 3-way verdicts (`7e65469`, 13/14 — alitas contradicted by the Cubeta combo PROSE block that ends with a price, defeating the priced-line proxy) and v3 mention-guard (`63fb8fc` — an anchor must be a line ABOUT the dish: ≥50% of the block's name-like tokens belong to the candidate's name; 14 tests). r4 verdict: fakes contradicted 5/5, keeps not deleted 9/9, 14/14 expectations — FULL PASS, first mechanism ever to kill the whole phantom class with zero real-dish deletions ($0.004 total). Full audit + production sketch parked in ledger "Eval 071 CLOSE"; NOT yet production promotion (one fixture, precision-only — recall gap untouched). **NEXT ACTION:** Santiago's live-cost approval for Eval 072 — nested tile-3 density probe per eval-069 spec + ruling 8's ≥25% overlap update (~$0.24/run, ×3 hard cap ≈ $0.72, stop on first failure); on approval the planner writes the executor task block. After Eval 072: production co-location wiring proposal (edge-side, dense scans only, fail-open) + polloteria ×3 through the production path (1e) + detector trigger (1f).
- **2026-07-17 (Eval 071 RAN: 5/5 fakes killed on printed evidence, 8/9 keeps verified; 3-way-verdict ruling PENDING):** Santiago re-created `.env.local`; executor launched r1 (the one paid OCR call, $0.004 — response cached durable in `~/Downloads/MenusTesting/`), planner diagnosed the r1 zero-units parse (real API shape = flat `content`-key blocks), executor landed the fix (`8b67bd8`, 9/9 tests) and reran at $0: 162 tight line-level blocks; dense name lines carry name+grams+price in one block. Full audit in ledger eval 071. Headline: every fake was rejected by exact printed contradiction (incl. ensalada-verde's borrowed $70 printed just 104px away — block-level separation, NOT radius, is what catches it); the sole keep failure (alitas-phase2-b) is a TRUE finding — the printed Alitas card has no grams anywhere, the candidate's `125gr/315gr/525gr` are model-added, so the check correctly refused an unprinted claim that my case fixture wrongly expected as printed. **NEXT ACTION — TWO USER RULINGS REQUIRED (both $0):** (a) ORACLE-CHANGE alitas-phase2-b: expect UNVERIFIABLE-keep instead of colocated-true; (b) adopt 3-way verdict semantics — VERIFIED / CONTRADICTED (only a PRICED anchor block printing a different value of a claimed field type may contradict; prose anchors cannot) / UNVERIFIABLE (keep card, flag unprinted fields, never delete). Dry-check against the r2 dump says 14/14 under these semantics → would meet ruling 8's release-gate tier on this replay. On approval: planner issues the executor task block (TDD the 3-way matcher, update cases fixture, $0 rerun), then ledgers and proposes the production-integration design.
- **2026-07-17 (Eval 071 harness BUILT + verified; awaiting .env.local to run):** Santiago approved proceeding. Pricing verified on mistral.ai/news/ocr-4: $4/1k pages standard → the one-time OCR call ≈ $0.004; Phase-B iterations are $0 (response cached to `~/Downloads/MenusTesting/polloteria.mistral-ocr-eval071.json`). Built directly by the planner (no executor round-trip): `scripts/fixtures/polloteria.eval071-cases.json` — 14 candidate cases derived from the eval-069 dump payloads (5 fakes expect co-location FALSE; 4 real-keeps + 5 control-keeps expect TRUE; includes the boneless-jr price-only and buffalo grams-only discriminators); `scripts/probe-colocation-eval071.ts` — Phase A cached single `POST /v1/ocr` (`mistral-ocr-latest`, `include_blocks: true`, current API verified via docs), Phase B deterministic block-level co-location (≥60% fuzzy name-token match anchors a block, then EVERY claimed field — price token and `NNNgr` — must be present in that same block); 8 unit tests in `probe-colocation-eval071_test.ts` — `deno check` PASS, 8/8 PASS, zero existing files modified. Known open risk: real OCR block granularity is unknown until the call — the script warns when <5 blocks (verdicts invalid; a $0 line-clustering v2 would follow). Gate unchanged from ruling 8: 5/5 fakes rejected + 9/9 keeps held → release-gate tier; ≥4/5 with all keeps held → advisory tier; any keep rejected → tighten or reject. **NEXT ACTION:** Santiago creates `.env.local` (OPENAI_API_KEY + MISTRAL_API_KEY) in the worktree, then run once: `deno run --env-file=.env.local --allow-read --allow-write --allow-net=api.mistral.ai scripts/probe-colocation-eval071.ts` (~$0.004); planner audits the verdicts + block granularity against the photo before any gate claim, then ledgers Eval 071.
- **2026-07-17 (Eval 070 — external research reviewed; Phase-1 re-sequenced by ruling 8; worktree rescued):** Santiago ran the external deep research requested in the 2026-07-15 recap entry (report archived at `docs/superpowers/horizontal-menus/2026-07-15-external-research-dense-menus.md`). Planner assessment: the report independently converges on eval-069's nested-tile recommendation and supplies the one untried mechanism — spatial co-location verification backed by a dedicated layout-OCR's region boxes; it validates the semantic-verifier rejections (evals 063–069) as correct elimination and warns VLM self-reported boxes are unreliable on dense small text. Santiago RATIFIED the new order (sequencing only, NOT live-cost approval): **Eval 071 = harness-only co-location replay first** (one Mistral-OCR layout pass ≈ cents + $0 offline geometry iterations against archived Eval-068/069 candidates; predeclared gate: 5/5 fakes rejected + 3/3 controls kept → becomes the Phase-1 release gate; 4/5 with controls kept → advisory only; any control rejected → reject the architecture), **then Eval 072 = nested tile-3 probe** (~$0.72 cap, spec updated to ≥25% sub-crop overlap), then ×3 production-path confirmation. Guided capture + full hybrid layout-first pipeline DEFERRED post-release; bake-off gains layout-first candidates. Mistral's role = coordinates provider, NOT the extractor swap framed by the unconcluded 2026-06-23 evaluation. Worktree incident: the tmp cleaner had destroyed the `.git` link + 121 tracked files; link recreated, files restored from HEAD, the stranded eval-069 doc aftermath rescue-committed as `3b39534`. Root cause found (macOS nightly `tmp_cleaner`/`dirhelper`, 3-day unused-file rule; full wipe on reboot) → the worktree was MOVED the same day to `/Users/santiagoaguirre/Desktop/CODING/menu-scan-app/.worktrees/extraction-eval-harness` (`git worktree move`; `.worktrees/` gitignored in main; rule 5 + executor hard rules updated: push after every commit). Casualty: untracked `.env.local` (OPENAI_API_KEY) was reaped — Santiago must re-create it before the next live run; all eval dumps are SAFE (probes write to `~/Downloads/MenusTesting/`, never the worktree). **NEXT ACTION:** planner writes the Eval 071 executor spec (co-location replay harness: OCR-box fetch + deterministic geometry checker, TDD, checkpoint before the single paid OCR call; relay cost approval in the task block). No production change until both probes report.
- **2026-07-15 (roadmap recap + reusable executor/research prompts requested):** Santiago ruled that abandoning dense horizontal support is not an option because most observed horizontal menus are dense. He is open to grounding BOTH remaining hypotheses with results: (1) upstream nested 2×2 subdivision of only the fake-producing tile; (2) a genuinely spatial/card-ownership verifier. This is not yet live-cost approval: each needs an isolated design, harness checkpoint, estimate, and explicit go-ahead. A third technical alternative was surfaced for research: ask users for 3–4 higher-resolution overlapping close-ups of one dense menu, but freehand capture risks cutting side items/full columns, so any viable version must be guided and coverage-validated. Eval 069 remains final: pairwise semantics is rejected, no rerun. **NEXT ACTION:** deliver the requested roadmap summary plus general zero-context executor and external-research prompts; then choose research/probe sequencing. Current combined estimate if both prior probe concepts are eventually approved is up to ~$0.96 (~$0.72 nested tiles + ~$0.24 spatial verifier), but no paid call or code begins from this statement alone.
- **2026-07-15 (Eval 069 complete; paired verifier REJECTED):** The approved manual replay ran once and stopped on its first failure after exactly 2 calls (~$0.08). It correctly dropped 2/5 known fakes (`Boneless de Coliflor 350/$150`, `Boneless Jr 200/$95`) and kept all 3 real/Phase-2 controls, but incorrectly kept 3/5 fakes as `neither`: `Buffalo 350/$150`, `Ensalada Verde 350/$70`, and `Nuggets de Coliflor 200/$132`. Dump: `polloteria.paired-ambiguity-eval069-r1.actual.json`; runs 2–3 were correctly skipped. Diagnosis: those three candidates are coherent plausible dishes/size variants in isolation; only spatial ownership shows their fields came from neighboring cards, so conservative pairwise semantics cannot delete them safely. Per the predeclared gate, reject this architecture: no rerun, prompt tuning, generic pair selector, or weaker keep bias. **NEXT ACTION — USER COST APPROVAL REQUIRED:** recommended upstream density probe: freeze the three clean Eval-068 parent tiles and replace only fake-producing tile 3 with nested existing-geometry 2×2 extraction; 4 extraction + up to 4 verifier calls ≈ $0.24/run, x3 hard max ≈ $0.72, stop on first fake/missing-card failure, raw trace + photo audit. Alternatives: another spatial verifier (~$0.24 max, not recommended after this failure) or stop horizontal support ($0). No production change or detector work until Santiago decides.
- **2026-07-15 (Eval 069 manual launch APPROVED after zero-cost sandbox failure):** Santiago explicitly approved one outside-sandbox manual launch after the executor's first attempt failed before Deno started. The experiment and cost authorization are unchanged: two calls/replay, up to three replays, stop on the first mismatch/error; estimated ~$0.08/replay and hard max ~$0.24. This approval is only for a single process launched from Santiago's normal Terminal; it does not authorize executor-sandbox retries, concurrent launches, design changes, or any production implementation. **NEXT ACTION:** commit the two pending brief entries, then Santiago runs the exact detached command once in normal Terminal, waits for that PID, and returns the complete log plus every Eval-069 dump. Planner diagnoses and ledgers the result before any next design or code decision.
- **2026-07-15 (Eval 069 launch blocked by executor sandbox; $0 spent):** Launch-authorization commit `37683b4` landed and the worktree was clean. The executor issued the detached command once and reported PID 77830, but zsh failed before Deno started because the sandbox denied creation of `/tmp/probe-eval069.log` (`operation not permitted`; the accompanying `nice(5)` warning was non-causal). Independent planner checks confirm PID 77830 is absent, the log does not exist, no `polloteria.paired-ambiguity-eval069-r*.actual.json` dump exists, and the branch is clean/ahead 13. Therefore zero replay calls completed, no API request was made, and cost was $0; this is an environment-launch failure, not an Eval-069 result. **NEXT ACTION — USER RULING REQUIRED:** recommended: authorize one manual launch from Santiago's normal terminal outside the executor sandbox under the unchanged estimate (~$0.08/replay, hard max ~$0.24); alternative: abandon Eval 069 with no spend. The prior no-relaunch rule prevents another attempt until Santiago explicitly decides. No code or experiment-design change is proposed.
- **2026-07-15 (Eval 069 checkpoint VERIFIED; approved replay launch relayed):** Correction commit `b8ab3c8` was independently reviewed after harness commit `3a674bb` and ruling/ledger commit `2079812`. The redundant literal schema suffix is gone, `PAIR_PROMPT` ends exactly at `Never drop both candidates.`, and strict `PAIR_SCHEMA` remains in `response_format`; the existing test now pins that ending. Commit boundary is exactly the two harness files plus the prior required brief entry. Fresh planner verification: formatting PASS, `deno check` PASS, combined suite 73/73, worktree clean/ahead 12 before this required log append, and `1ab93f5..HEAD` has zero production/fixture/`probe-tiles.ts` diff. Candidate/expected-answer separation, two calls/replay, two high-detail images/call, stop-on-first-mismatch/error, and three-replay cap remain intact. **NEXT ACTION:** executor commits this progress entry, then launches `scripts/probe-paired-ambiguity.ts` once detached under Santiago's existing approval (~$0.08/replay, hard max ~$0.24), returning the complete untouched log plus every Eval-069 result dump and modification time. Do not relaunch on any failure and do not change production or select an implementation until the planner diagnoses and ledgers the replay.
- **2026-07-15 (Eval 069 checkpoint reviewed; one prompt-control correction required):** Manual commits `2079812` (Eval-068 ledger + Eval-069 approval) and `3a674bb` (paired replay harness) were independently reviewed. Commit boundaries are correct; worktree is clean/ahead 11; fresh `deno fmt --check` and `deno check` pass; combined suite is 73/73; and `1ab93f5..HEAD` has zero production/fixture/`probe-tiles.ts` diff. The harness resolves all archived candidates before reading the API key, sends exactly two high-detail images per group, strips expected verdicts from requests, runs exactly two calls/replay, stops on the first mismatch/error, and caps at three replays. One experiment-control mismatch blocks launch: `PAIR_PROMPT` appends a redundant literal `SCHEMA` JSON block even though the approved exact prompt ends at `Never drop both candidates.` and the same strict schema is already supplied through `response_format`. **NEXT ACTION:** executor removes only that redundant prompt suffix, pins the approved prompt ending in the existing test, reruns the same checks, commits the harness-only correction, and HARD STOPS. No API call or paid launch until the planner verifies that correction; the already-approved hard cap remains ~$0.24.
- **2026-07-15 (Eval 069 paired ambiguity replay approved):** Santiago approved the recommended harness-only pairwise diagnostic, estimated ~$0.08/replay and hard-capped at ~$0.24 total (2 two-image calls × up to 3; no extraction calls). Design: compare each of the five Eval-068 fake source candidates with its accurate overlap copy; output drop-A/drop-B/neither with strict keep-on-uncertainty. Distinct printed dishes plus the Phase-2 Alitas representation are keep-both controls. **NEXT ACTION:** executor commits the pending Eval-068 ledger/brief updates, TDDs only `scripts/probe-paired-ambiguity.ts` + its test, proves exactly two calls/replay and zero production diff, then HARD STOPS before paid launch for planner checkpoint review. Do not modify or launch `probe-tiles.ts`, production extraction/verifier/merge/postprocess, geometry, prompts, or fixtures.
- **2026-07-14 (Eval 068 diagnosed; batching not sufficient):** One approved run (~$0.23) stopped at items 46/40, options FP, and one S&H section miss; categories+grams passed. Batching worked mechanically (tile 3 = 12/12/10) and rejected `Boneless El Jr`, but photo/raw audit found five other surviving fake source candidates: four visible extras (`Boneless de Coliflor 350/$150`, `Buffalo 350/$150`, `Ensalada Verde 350/$70`, `Nuggets de Coliflor 200/$132`) plus `Boneless Jr 200/$95` under Crispy Chicken, which merged into the real Kids item and caused the option FP. Exact final count = 39/40 real food cards + 2 tolerated Paletas + 1 Phase-2 Alitas duplicate + 4 visible fakes = 46. Root cause is now isolated: the current verifier receives names only and is explicitly told to ignore weights; candidates that correspond to a real base dish but borrow fields from neighbors pass even in 10–12-name batches. **NEXT ACTION — USER COST APPROVAL REQUIRED:** recommended harness-only paired-card ambiguity replay using archived candidates + reconstructed tiles, two calls/replay ×3, distinct-real + Alitas keep-both controls, stop on first wrong verdict; estimated ~$0.08/replay, hard max ~$0.24. No production change, no new extraction calls, no smaller-batch retry, and no detector work until Santiago decides.
- **2026-07-14 (Eval 068 checkpoint verified; launch relayed):** Documentation ruling commit `b15d2b4` and implementation commit `7a880ed` independently reviewed. Semantic diff is the approved single variable only: verifier lists >24 split into ordered ≤12-name batches against the same tile; `runGroupedExtraction` routes tile verification through it; Eval-068 dump tag updated. No verifier prompt, extraction prompt, geometry, merge, postprocess, or fixture change. Fresh planner verification: `deno fmt --check` PASS, `deno check` PASS, analyze-menu 70/70 PASS, probe-output 1/1 PASS; implementation checkpoint was clean/ahead 8 before this required progress-log append. **NEXT ACTION:** executor commits this log append, then launches the already-approved detached Eval-068 probe (up to 3 runs, estimated hard-plan max ~$0.68), returning the complete untouched log and every Eval-068 raw dump. Planner must photo-audit every run for inventions even if the count scorer passes; do not start detector-trigger work until the fake-item audit is zero and the Phase-1 gate is judged.
- **2026-07-14 (Eval 068 approved; fake items remain hard blocker):** Santiago approved BOTH decisions: (1) printed Grande/Jumbo and Alitas multi-representations stay Phase-2-owned until mandatory Shape A (not shippable duplicates; Phase 2 still must fold them); (2) unprinted items remain release-blocking and the next single-variable experiment is overload batching — verifier lists >24 split in menu order into ≤12-name calls against the same tile. Polloteria estimate ~$0.23/run, x3 hard-plan estimate ~$0.68 APPROVED; raw dump + photo audit required even on numeric PASS. **NEXT ACTION:** hand executor the TDD/checkpoint block for `verifyTileItemsBatched` + runGroupedExtraction wiring + Eval-068 dump tag only; collect tests/diff and verify no prompt/merge/geometry/oracle change before relaying the approved detached probe.
- **2026-07-14 (Eval 067 trace complete):** One run (~$0.18) failed 49/40 and stopped as designed; other four dims green. Raw boundary trace is decisive: tile candidate loads = 19/17/**36**/10 food; only overloaded tile 3 rejected anything (one Ensalada) and it sourced every survivor. Exact final decomposition = 39 expected dishes present + 2 real tolerated Paletas + 4 printed Phase-2 size-variant overrepresentations (Grande/Jumbo + combined-and-split Alitas) + 4 unprinted tile-3 phantoms (`Boneless Jr 150gr/$119`, `Crispy 300gr/$159`, `Ensalada Verde 350gr/$70`, `Nuggets de Coliflor 200gr/$99`) = 49. This proves a future 45/40 count PASS could still hide all four phantoms. **NEXT ACTION — USER RULING + COST APPROVAL REQUIRED:** recommended ruling: printed size-variant overrepresentations remain Phase-2-owned (mandatory Shape A), but unprinted items remain Phase-1 blockers; recommended one-variable probe: only lists over 24 candidates split into menu-order v3 batches of max 12 against the same tile (trace impact: tile 3 becomes 3 verifier calls; 10/17/19 lists unchanged), raw-dumped/photo-audited x3, estimated ~$0.23/run/~$0.68 total. Alternatives: pull Shape-A folding into Phase 1 (mixes phases; not recommended) or design a post-merge ambiguity verifier (more machinery; not recommended before testing the proven 36-name overload). Do not run or code until Santiago decides.
- **2026-07-14 (Eval 067 launch authorized):** Checkpoint 1 VERIFIED. Repair commit `126f4da` removed the accidentally tracked `supabase/.temp` files, ignored generated state, landed the approved Alitas pin, and committed Eval-066 docs; trace commit `d491a61` added the raw-response dump helper/test and harness-only stop-after-first-failure behavior. Fixture self-check ALL PASS; red test correctly failed missing-module; final `deno check` PASS + 68/68 tests; `369e98b..HEAD` has NO `supabase/functions/analyze-menu` diff; worktree clean/ahead 6. **NEXT ACTION:** executor launch block handed with Santiago's existing approval (~$0.18/run, hard max 3/~$0.55). Collect untouched `/tmp/probe-eval067.log` plus every Eval-067 dump and decoded four-tile raw item lists; then planner traces each residual to extraction → v3 verdict → merge. Do not select a fix or start detector work before that analysis.
- **2026-07-14 (Eval 067 diagnostic approved):** User approved Option 1 with a hard live-cost cap of ~$0.55 (about $0.18/run, stop after first scored failure). Executor Checkpoint-1 task block handed off: repair Eval-066 staging (`supabase/.temp` accidental commit + uncommitted approved Alitas pin), commit the already-written Eval-066 ledger/brief updates, then TDD a harness-only trace that always saves final items + existing `raw_response` under Eval-067-specific filenames and stops after the first scored failure. **NEXT ACTION:** collect executor Checkpoint 1 (commits + tests + diff); verify no extraction/verifier/merge behavior changed; only then relay the already-approved detached launch. Do not spend API cost before that planner checkpoint.
- **2026-07-14 (Eval 066 diagnosed):** I9 landed and improved its three target classes, but the forced 2x2 probe was only 1/3 (r1 all green; r2 47/40; r3 46/40; other four dims green 3/3). Photo adjudication shows the residual is duplicated Alitas plus cross-tile word/field recombinations (`Boneless El Jr`, `Tender de Coliflor`, `Cheesy`/`Boneless P`, `Ensalada Verde 350gr/$70`, `Nuggets de Coliflor`). Root cause is scope: v3 verifies independent name existence per tile, so two representations of one real card can both pass; the prompt already contains the generic two-dish-mashup warning. Evidence gap: the probe omits existing raw tile responses and writes only failing final dumps, so current r1 is unauditable (on-disk r1 is stale). Repo checkpoint also needs repair: `369e98b` accidentally contains two `supabase/.temp` files and omitted the approved Alitas fixture pin, which remains uncommitted. **NEXT ACTION:** await user approval for Eval 067 diagnostic trace: $0 repo/harness-only repair + always save final/raw tile outputs, then run until first scored failure (about $0.18/run, max 3/~$0.55). Do NOT choose another prompt/merge fix or start the detector trigger until the source-tile trace is analyzed.
- **2026-07-14 (late night):** Verifier v3 LANDED and KEPT (commit 00dec3c; rejections 0/2/1, only Ensaladas mangles — no real-dish over-deletion). Ensaladas oracle tolerance LANDED (2f93a9a; restore clause written into ROADMAP Phase 2). v3 probe = ledger eval 065: FAILED 3/3 but for NEW reasons — v2's random over-deletion had been masking a tile-twin/echo overcount (51 & 50 vs 45 ceiling). Dump decomposition → 3 deterministic fixes, ALL USER-APPROVED with re-probe ~$0.55: (I9a) dropOptionEchoItems (Paletas flavors; USER RULING: no combo machinery, price guard only, bias to keeping standalone dishes); (I9b) twin-fold relax — section-agnostic + near-name, grams-equality guard stays; (I9c) remapTruncatedSectionTitles (exactly-one-superset guard); plus ORACLE (approved): "Alitas" unchecked options pin (becomes required Shape-A pin at Phase 2 kickoff). EXECUTOR TASK BLOCK for all of this ("Eval 066") was handed to Santiago — NEXT ACTION: collect the executor's checkpoint/final report, ledger eval 066, analyze. If items/options go green, residual watch-items are (E) run-1-class heading miss (no tile read "Sandwiches & Hamburguesas"; contingency = tile overlap bump OR one heading-focused tile-prompt sentence) and (C) inventions ("Crispy (500gr)"/"Chicken Bacon"; contingency = one verifier-prompt sentence) — iterate ONE variable if either recurs. After polloteria 2×2 is 3/3 → detector trigger probe (aspect-scoped P1 density suffix, ~$0.90 ALREADY approved) → Phase 1 exit.
- **2026-07-14 (evening):** Phase 1c iteration 3 pending. Verifier v2 probe (ledger eval 064): items/options/categories/grams GREEN 3/3 on polloteria; ONLY section_context fails (Ensaladas — Phase-2 coupling, see eval 064). NEXT ACTION: user approval was requested for (a) verifier v3 = per-tile, name-only, pre-merge, price machinery removed; (b) TEMPORARY oracle tolerance: polloteria `sections` "Ensaladas" → `section_headers` + drop its pins, restore at Phase 2 close; (c) re-probe ×3 ~$0.55. If approved → executor task block for v3; if v3 holds → detector trigger (aspect-scoped P1 suffix, ~$0.90 ALREADY approved) → Phase 1 exit (polloteria 3/3 through the full production path incl. detector).
- **2026-07-14:** Verifier v1 over-deleted real dishes (eval 063); v2 designed + run (eval 064). parseItemGrams comma bug fixed ("1,200gr"→1200). RULING: duplication/invention = release-blocking; verification pass pulled INTO scope; polloteria over-band stays +5.
- **2026-07-13/14:** Hygiene ladder landed (evals 061-062): tolerated_categories scorer field; dropBannerEchoOptions; nullPriceNoteSections; tile-pass twin fold + truncation drop (merge.ts); misread pin retargets. Geometry A/B: 2×2 WINS, 4×1col REJECTED.
- **2026-07-13:** Detector diagnosis (eval 060): TOTAL blind spot on landscape (dense=false + crop_direction=none 3/3). Phase 0 CLOSED (eval 059): 3 fixtures built + self-consistent.

## Where you are in the plan hierarchy

```
Menu Scan App development (AGENTS.md — product, stack, rules)
└─ Core feature: OCR extraction hardening
   (MASTER ROADMAP: main repo docs/superpowers/plans/2026-07-04-ocr-extraction-master-roadmap.md
    — Features 1-4 CLOSED; pre-release critical path #1 wiring ✅, #2 auto-cutter ✅, #3 compression ✅)
   └─ Critical-path #4: Horizontal/landscape menus  ← YOU ARE HERE
      (this folder: ROADMAP.md = phases/rulings; this brief = entry point + log)
      └─ after this: #5 Stage-2 enrichment benchmark → model bake-off
```

## The planner ↔ executor split (KEEP RUNNING IT)

Two LLM sessions, one human (Santiago):
- **PLANNER (this brief's primary audience):** analysis, brainstorming, probe design, dump diagnosis, gate judgment, ledger/roadmap/brief writes, authoring executor task blocks. Uses the superpowers skills (brainstorming / systematic-debugging / writing-plans) at the corresponding moments.
- **EXECUTOR (separate zero-context session; Santiago pastes task blocks to it):** writes code TDD-first, runs self-checks/`deno check`/`deno test`/`tsc`, commits, launches detached runs, monitors logs, reports RAW results verbatim. Executor task blocks must be self-contained (exact code, exact commands, expected output) and always end with hard stops: report back at checkpoints; never launch multi-run gates without an explicit relayed cost approval; never commit in the main checkout.
- **SANTIAGO decides:** all ORACLE-CHANGEs (fixture truth), convention rulings, cost approvals, device scans. When evidence needs a ruling, the planner presents options with a recommendation and waits.

## Non-negotiable operating rules (hard-won; violating them wasted real money)

1. Read the MASTER ROADMAP's "Strategy Rules" + "Lessons learned — GENERAL rules" (10 rules) before any prompt/eval work. Highlights: one run proves nothing (temp-0 ≠ determinism); probe cheap before gating expensive; prompt edits have NON-LOCAL side effects — one sentence at a time, mode-scoped to where evidence was measured; adjudicate from the MENU PHOTO, never from model output; fix at the layer where information still exists; instrument before theorizing.
2. Long live runs: `nohup … > log 2>&1 &` then poll the log file. NEVER foreground (10-min tool timeout kills runs), never pipe live output.
3. Validate offline ($0: score-dump vs archived dumps, self-checks) before paying for live runs. `OPENAI_API_KEY` is in the worktree's `.env.local` (gpt-4o, Tier-1 30k TPM / 90k TPD — campaigns exhaust the daily window; both prior 3/3 closures were daytime runs).
4. Every experiment gets a ledger entry (same format as evals 001-064). Every planner session updates the ⚡ PROGRESS LOG above.
5. The worktree `/Users/santiagoaguirre/Desktop/CODING/menu-scan-app/.worktrees/extraction-eval-harness` (branch `feat/extraction-eval-harness`, pushed to origin) is the ONLY place to commit. It MOVED here 2026-07-17 from `/private/tmp` after macOS's nightly tmp cleaner (deletes files unused ≥3 days; full wipe on reboot) destroyed the old checkout twice. If it ever vanishes: `git worktree prune -v` then `git worktree add .worktrees/extraction-eval-harness feat/extraction-eval-harness` from the main repo, and ask Santiago for `.env.local` (untracked — the 07-17 incident reaped it; it must be re-created before any live run). Commit early and often, and push to origin after EVERY commit. The MAIN checkout (`/Users/santiagoaguirre/Desktop/CODING/menu-scan-app`, branch feat/selectable-options) is device-builds only — NEVER commit the client slice there (docs-only commits allowed).
6. Vertical safety during iteration is proven BY CONSTRUCTION (portrait behavior unit-pinned byte-identical; new rules scoped to the tile pass / landscape mode), not by burning gates. The 6 vertical menus re-run live only at the Phase-4 combined exit gate (~$1.35-1.50/attempt, approval per attempt).
7. Generalization rule: no hardcoded menu-specific counts/names/geometry in production code. Fixture-level tolerances are the mechanism for menu-specific quirks.

## File map (all paths)

**This folder (worktree):** `docs/superpowers/horizontal-menus/`
- `DELEGATION-BRIEF.md` — this file (entry point + progress log)
- `ROADMAP.md` — phases 0-4, rulings (Shape A, aderezos, fixture counts, verification pass scope), architecture decisions, cost policy

**Governing docs:**
- `/Users/santiagoaguirre/Desktop/CODING/menu-scan-app/CLAUDE.md` + `AGENTS.md` — READ AND FOLLOW STRICTLY (AGENTS.md also carries the critical-path status line)
- Main repo `docs/superpowers/plans/2026-07-04-ocr-extraction-master-roadmap.md` — MASTER roadmap (strategy rules, lessons, release scope)
- Main repo `docs/superpowers/diagrams/menu-extraction-pipeline.md` — pipeline + verbatim P1/P2 prompts (update on close, re-copy to ~/Downloads)

**Worktree evidence trail:**
- `docs/superpowers/extraction-iteration-ledger.md` — EVERY experiment ever (horizontal work = evals 059-064+; read at least those before proposing anything)
- `docs/superpowers/specs/` + `docs/superpowers/plans/` — prior specs/plans (ticket #3 compression etc.)

**Code (worktree-relative):**
- `supabase/functions/analyze-menu/extract.ts` — P1 + suffixes, runPagedExtraction (phase-1, `{needs_crops}` on dense), runGroupedExtraction (tile groups; verification pass lives here), extractWithRetry
- `supabase/functions/analyze-menu/merge.ts` — mergeItemSources (tile-pass twin fold + truncation drop live here), postprocess.ts (hygiene chain, `--self-check`), index.ts (edge handlers, 10M-char per-photo cap)
- `scripts/eval-027-live.ts` — THE gate runner (GATE_DIMS all 5 + detector asserts; production-mirror input via photo-input.ts)
- `scripts/eval-extraction.ts` — scoreMenu + self-check; `scripts/score-dump.ts` — offline dump scorer; `scripts/photo-input.ts` — production-mirror input; `scripts/extract-draft.ts` — new-menu drafts; `scripts/probe-fidelity.ts`, `scripts/probe-detector.ts`, `scripts/probe-tiles.ts` — probes
- `scripts/fixtures/*.expected.json` — oracles (bistro/polloteria/guest-house = the landscape three; NEVER edit without a Santiago ruling)
- Client slice: `src/lib/compressImage.ts`, `analyzeMenu.ts`, `adaptiveExtraction.ts` (gridCropRects), `src/app/(tabs)/index.tsx`, `src/components/scan/GalleryButton.tsx`, `src/store/scan.store.ts`

**Assets:** `/Users/santiagoaguirre/Downloads/MenusTesting/` — 6 vertical photos + `BistroMenu.png`, `PolloteriaMenu.png` (dense landscape), `GuestHouseMenu.png` (English landscape); user-corrected truths `*.png.draft.json`; `*.actual.json` = free offline validation dumps (check mtimes — overwritten per run).

## Iteration log — DO NOT REPEAT THESE (full detail in ledger evals 059-064)

| Tried | Result | Lesson |
|---|---|---|
| Full-page extraction of dense landscape (polloteria) | 39-46 of 55 items, names mangled, detector silent | Landscape rescale (768px SHORTEST side = height) crushes small print; GuestHouse survives only because its type is larger |
| 4×1col column-strip tiles | REJECTED — loses left-column sections 3/3, unstable counts | Don't retry column strips |
| 2×2 tiles (portrait-proven geometry) | WINNER — full coverage; needed the hygiene ladder on top | Keep 2×2 for landscape |
| Widening bands to absorb phantoms | REJECTED BY RULING — duplication/invention is release-blocking (waiter-embarrassment test) | Fix, don't tolerate; band stays +5 |
| Verifier v1 (one boolean, drop on false) | Over-deleted real dishes (Dedos de Queso, all Ensaladas) | Price-mismatch ≠ deletion; field flakes are acceptable, missing dishes are not |
| Verifier v2 (two-field, twin-tie price drops) | price path NEVER fired; name path high-variance (3 vs 12 drops across runs) | 50 names × 4 overlapping images in one call is too hard; price/twin handling belongs to the merge rules |
| Verifier v3 (per-tile, name-only, pre-merge) | KEPT — rejections 0/2/1, only Ensaladas mangles (eval 065) | Verifier's blind spot BY DESIGN: word-mashup inventions ("Crispy (500gr)") survive because every word IS printed |
| Post-merge verification as an overcount fix | v2 only LOOKED like it controlled counts — it was randomly deleting ~12 names | Overcounts are merge-rule problems (twins/echoes), not verification problems |
| More P1 prompt rules naming dish patterns | (Earlier eras: v4, v7.1) REGRESSED — priming effects | Never name specific dish/box patterns in P1 |
| Consistency-intersection for phantoms | REJECTED (nikkori era): stable phantoms survive intersection | — |

**Working set that must not regress (all TDD'd, tile-pass-scoped):** tolerated_categories (scorer), dropBannerEchoOptions, nullPriceNoteSections, twin fold + truncation drop (merge.ts), parseItemGrams comma fix, misread pin substrings ("tiBoneless", "Megach", Cabeza/Cubeta + "El Tender" unchecked keys), verifier v3 (00dec3c). IN-FLIGHT with the executor (task block "Eval 066"): dropOptionEchoItems + remapTruncatedSectionTitles (postprocess.ts, tile-chain-wired), twin-fold relax (merge.ts), "Alitas" unchecked pin — confirm landed + probe results before assuming.

## Known watch-items / open threads

- Ensaladas (polloteria) = size-variant cards; blocked on Phase 2's extraction convention (Shape A ruled). Temporary tolerance pending approval (see PROGRESS LOG).
- Detector trigger (1f): RESOLVED by ruling 24 (2026-07-18) — deterministic tile-all-horizontal: landscape pages (`photo_dims` w>h) → `needs_crops`, no model call; `LANDSCAPE_PROMPT_SUFFIX` deleted (evals 086–088 proved cheap dense discrimination infeasible on wide menus); `photo_dims` plumbing retained; header parsing remains REJECTED (EXIF-rotated camera JPEGs swap raw dims). Implementation + B-ladder probes (bistro-through-tiles is the risk to watch) + production wiring still pending — Phase 1's remaining exit blockers. Wide-menu dense discrimination = post-release cost optimization (master roadmap POST-RELEASE list).
- Phase 3 rotation NOT built and now known to be REQUIRED for the camera core flow (capture screen always yields portrait photos; horizontal menus arrive as sideways-text-inside-portrait — ruling 23). Ground rules locked (ruling 22): content-based detection only (never device sensors), direction-aware rotation (90CW/90CCW/180, never hardcoded). Order forced: 1f first, then Phase 3.
- Stable misreads (Cabeza/469, TortiBoneless/480, El Tender*) = model-level, tolerance-managed; candidates for the post-#5 model bake-off (polloteria + guest-house join its fixture set).
- Rotation arm (Phase 3) untouched; combined 9-menu exit gate (Phase 4) untouched.
