You're continuing the Horizontal Menus roadmap (critical-path #4) of Menu Scan's OCR extraction hardening. Full context, hierarchy, rules, the planner↔executor protocol, and the iteration log live in /private/tmp/menu-scan-app-extraction-eval-harness/docs/superpowers/horizontal-menus/DELEGATION-BRIEF.md — read it IN FULL before doing anything; it overrides your defaults except the repo's /Users/santiagoaguirre/Desktop/CODING/menu-scan-app/CLAUDE.md and AGENTS.md, which also govern — read both and follow them strictly. (If the worktree is missing from /private/tmp, the brief's rule 5 tells you how to recreate it from branch feat/extraction-eval-harness.)

Start with the brief's "⚡ PROGRESS LOG" section — its newest entry names the current phase state and the NEXT ACTION; continue from exactly there, not from what you infer elsewhere. The phase structure, user rulings, and exit gates are in ROADMAP.md (same folder); every experiment ever run is in docs/superpowers/extraction-iteration-ledger.md (read at least evals 059 onward) — do NOT re-run anything in the brief's "DO NOT REPEAT" table.

You are the PLANNER: you analyze, design probes, diagnose dumps, propose rulings, and write self-contained task blocks that Santiago pastes into a separate zero-context EXECUTOR LLM (which writes code TDD-first, commits on the worktree branch, launches detached runs, and reports raw results back). Keep that split. Santiago personally decides all fixture/oracle changes, extraction-convention rulings, and every live-run cost approval (state the $ estimate before asking) — present options with a recommendation and wait. Ledger every experiment in the iteration ledger AND append a PROGRESS LOG entry to the brief before your session ends, so the next zero-context planner can continue without repeating work.

Begin: read the governing files above, then report your understanding of the current NEXT ACTION and your proposed step — do not spend money or change code before Santiago confirms.
