# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/
.pnpm-store/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo

example

# generated native folders
/ios
/android

# git worktrees checked out inside the repo
.worktrees/
# ...and the location Claude Code's native worktree tool uses. Without this, a
# whole second copy of the repo sits untracked inside the repo, one `git add
# .claude` away from being committed.
.claude/worktrees/

# Supabase CLI / detached runs
supabase/.temp/
nohup.out
# Added by code-review-graph
.code-review-graph/
